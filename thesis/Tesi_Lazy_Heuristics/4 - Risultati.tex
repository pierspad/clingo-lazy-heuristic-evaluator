\chapter{Results}
\label{sec:results}

\section{Experimental Setup and Dataset}
\label{sec:dataset}

All experiments were executed on dedicated HPC cluster nodes requesting 4 cores, with hard limits of 600 seconds and 30~GB of RAM per run enforced by \texttt{runlim}. Both backends were compiled on the test hardware and the version of SWI-Prolog used was 10.0.2.

The evaluation covers the benchmark suite defined in Section~\ref{sec:benchmarks}:
\begin{itemize}[noitemsep,topsep=2pt]
    \item BSP: $n \in \{10, 20, \ldots, 200\}$ ($20$ instances);
    \item PUP: \texttt{double-$N$}, with $N \in \{20, 40, \ldots, 200\}$ ($10$ instances);
    \item HRP: \texttt{house-$N$}, with $N \in \{2, 4, \ldots, 20\}$ persons ($10$ instances).
\end{itemize}

Across all benchmark families, timeouts represent the exclusive first failure mode (denoted \emph{T@$N$} in the summary tables), as every failing variant encounters the 600-second time limit before exhausting the memory ceiling. Memory exhaustion is confined to the BSP instances where $n \ge 180$, where base program grounding alone exceeds the 30\,GB budget, beyond all the first failures for BSP.

On BSP, search trajectories between C++ and Prolog coincide identically across all solved instances. On PUP and HRP there are minor discrepancies in choices that arise solely from the tie-breaking among equal-rank candidates.

\subsection{Measurement Reliability and Timing Variation}
\label{sec:time-reliability}

Tables~\ref{tab:overview-bsp}--\ref{tab:overview-hrp} summarize the outcome of the benchmark campaign, reporting the number of solved instances and the first failure point for each variant.
In the BSP benchmark, solver failures scale predictably with instance size: once a variant fails at a given size, it also fails for all subsequent sizes, with timeouts representing the first failure mode in all cases (between $n=70$ and $n=170$). On PUP, however, difficulty does not scale monotonically with size: \texttt{lc} times out on \texttt{double-80} yet successfully solves \texttt{double-100}. This behavior stems from the benchmark design, which provides only a single instance per size with significant variations in hardness between neighboring sizes; Section~\ref{sec:pup} analyzes this effect in detail using choice counts rather than runtimes.

\begin{table}[H]
\centering
\small
\begin{tabular}{lllll}
\toprule
 & \multicolumn{2}{c}{\textbf{native}} & \multicolumn{2}{c}{\textbf{Prolog}}\\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
\textbf{Variant} & \textbf{solved} & \textbf{first failure} & \textbf{solved} & \textbf{first failure}\\
\midrule
\texttt{gc\_noheur} & 15/20 & T@160 & 15/20 & T@160\\
\texttt{gc}        & 13/20 & T@140 & 13/20 & T@140\\
\texttt{ga}        & 6/20  & T@70  & 6/20  & T@70\\
\texttt{ga\_weak}   & 14/20 & T@150 & 14/20 & T@150\\
\texttt{la}        & \textbf{16/20} & \textbf{T@170} & \textbf{15/20} & \textbf{T@160}\\
\texttt{lc}        & 15/20 & T@160 & 14/20 & T@150\\
\midrule[\lightrulewidth]
\texttt{la\_aux}    & 6/20  & T@70  & 3/20  & T@40\\
\texttt{la\_co}     & \textbf{20/20} & \textbf{---} & \textbf{20/20} & \textbf{---}\\
\bottomrule
\end{tabular}
\caption{BSP campaign overview ($n=10..200$, 600\,s / 30\,GB per run).}
\label{tab:overview-bsp}
\end{table}

\begin{table}[H]
\centering
\small
\begin{tabular}{lllll}
\toprule
 & \multicolumn{2}{c}{\textbf{native}} & \multicolumn{2}{c}{\textbf{Prolog}}\\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
\textbf{Variant} & \textbf{solved} & \textbf{first failure} & \textbf{solved} & \textbf{first failure}\\
\midrule
\texttt{gc\_noheur} & 4/10  & T@100 & 4/10  & T@100\\
\texttt{gc}        & 3/10  & T@80  & 3/10  & T@80\\
\texttt{ga}        & \textbf{10/10} & \textbf{---} & \textbf{10/10} & \textbf{---}\\
\texttt{la}        & \textbf{10/10} & \textbf{---} & \textbf{10/10} & \textbf{---}\\
\texttt{lc}        & 4/10  & T@80 (non-mon.) & 2/10 & T@60\\
\bottomrule
\end{tabular}
\caption{PUP campaign overview (\texttt{double-$N$}, $N=20..200$, 600\,s / 30\,GB per run). Best results in bold.}
\label{tab:overview-pup}
\end{table}

\begin{table}[H]
\centering
\small
\begin{tabular}{lllll}
\toprule
 & \multicolumn{2}{c}{\textbf{native}} & \multicolumn{2}{c}{\textbf{Prolog}}\\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
\textbf{Variant} & \textbf{solved} & \textbf{first failure} & \textbf{solved} & \textbf{first failure}\\
\midrule
\texttt{gc\_noheur} & 10/10 & --- & 10/10 & ---\\
\texttt{gc}        & 10/10 & --- & 10/10 & ---\\
\texttt{ga}        & 10/10 & --- & 10/10 & ---\\
\texttt{la}        & 10/10 & --- & 10/10 & ---\\
\texttt{lc}        & 8/10  & T@18 & 7/10  & T@16\\
\bottomrule
\end{tabular}
\caption{HRP campaign overview (\texttt{house-$N$}, $N=2..20$ persons, 600\,s / 30\,GB per run).}
\label{tab:overview-hrp}
\end{table}

The maximum instance size solved provides different insights depending on the problem of choice. 
\begin{itemize}
    \item \textbf{On BSP}, the solvability frontier alone does not separate the approaches. Variants sharing the same base encoding (\texttt{gc\_noheur}, \texttt{la}, and \texttt{lc}) all stop between $n=150$ and $n=160$, constrained by the heavy grounding of the base program. What distinguishes them is search efficiency: \texttt{la} reduces solving time and conflicts to near zero (Sections~\ref{sec:bsp-runtime} and~\ref{sec:per-decision-cost}).
    \item \textbf{On PUP}, the Alpha-like dynamic guidance lets both \texttt{ga} and \texttt{la} solve the entire range up to $N=200$, whereas the other variants stall around $N=80$ under search space explosion. With search no longer the bottleneck, PUP isolates the cost of the heuristic representation itself: \texttt{la} halves both grounding time and peak memory with respect to the static aggregate unrolling of \texttt{ga} (\textbf{12.7\,GB} vs \textbf{24.6\,GB} at $N=200$).
    \item \textbf{On HRP}, the frontier is uninformative because nearly all variants solve the entire suite; the evaluation centers instead on decision quality and on the misguidance observed in \texttt{lc}.
\end{itemize}

Three further trends are visible in the tables. First, the static ground simulation (\texttt{ga}) fails on BSP already at $n=70$, nine sizes earlier than the baseline, under the combinatorial explosion of static unrolling. Second, the native C++ and Prolog backends yield identical frontiers whenever propagator queries remain cheap; where they diverge, the Prolog backend always fails earlier due to evaluation overhead, particularly for \texttt{lc}, where the propagator is queried tens of thousands of times. Third, \texttt{lc} shows two distinct failure modes of clingo-style aggregate evaluation over partial assignments: on BSP the heuristic never activates, because its aggregate conditions are not satisfied at any decision point, whereas on HRP delayed evaluation produces misleading suggestions that degrade search by orders of magnitude (Sections~\ref{sec:bsp-semantics} and~\ref{sec:hrp}).

\section{Reduction of the Ground Heuristic Component}

The primary goal of this lazy evaluation was to reduce memory spent on materializing heuristics during grounding. This reduction can be directly observed in the number of ground heuristic objects generated as a function of instance size $n$. In standard ASP encodings, directives such as:

\begin{minted}{prolog}
#heuristic b(X) : x(X), not c(X), S = #sum{Y : c(Y)}, W = (n+1)*S+X. [W, true]
\end{minted}

incorporate the dynamic aggregate value directly into the heuristic weight. Because the atoms \texttt{c(Y)} are choice atoms whose truth values are determined only during search, the grounder cannot compute the sum ahead of time. It must therefore instantiate a separate directive for every possible $(X,S)$ pair. For $X \in \{1,\ldots,n\}$ and $S$ ranging over all reachable subset sums (i.e., integers from $0$ to $n(n+1)/2$), the total number of ground directives generated by the two symmetric rules for \texttt{b} and \texttt{c} is:
\[
2 \cdot n \cdot \Big(\tfrac{n(n+1)}{2} + 1\Big) \;=\; \Theta(n^3).
\]

The lazy variants never perform this expansion. The aggregate is evaluated on the current partial assignment during search, so the materialized heuristic representation stays constant at two static template facts, regardless of $n$.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{combined_heuristics}
    \caption{Comparison between native ground heuristic objects and lazy heuristic facts.}
    \label{fig:combined-heuristics}
\end{figure}

\begin{table}[H]
\centering
\small
\begin{tabular}{rrrr}
\toprule
\textbf{$n$} & \textbf{Native heuristic objects} & \textbf{Lazy facts} & \textbf{Ratio}\\
\midrule
10  & $1{,}120$       & 2 & $560\times$\\
50  & $127{,}600$     & 2 & $63{,}800\times$\\
100 & $1{,}010{,}200$ & 2 & $505{,}100\times$\\
110 & $1{,}343{,}320$ & 2 & $671{,}660\times$\\
\bottomrule
\end{tabular}
\caption{Growth of the G\&S heuristic component against the lazy evaluated one.}
\label{tab:heur-growth}
\end{table}

This reduction reflects a smaller grounding footprint rather than eliminated computation: lazy evaluation shifts candidate generation to the solver's decision loop, whose runtime cost is analyzed in Section~\ref{sec:per-decision-cost}.

\section{BSP: Runtime Behaviour}
\label{sec:bsp-runtime}

For all variants sharing the base encoding without all the grounded heuristic directives (\texttt{gc\_noheur}, \texttt{la}, and \texttt{lc}), the base program grounding alone dominates overall runtime. At $n=120$, it generates $52.8$ million rules, requiring $160.9$\,s of grounding for \texttt{gc\_noheur}, $159.7$\,s for \texttt{lc}, and $154.2$\,s for \texttt{la}.

However, a clear difference emerges here from the two readings of negation: while \texttt{gc\_noheur} and \texttt{lc} require $44.1$\,s and $47.5$\,s respectively, \texttt{la} completes search in just $0.04$\,s. The total runtime curve for \texttt{la} therefore stays below every other variant sharing the same base encoding across the whole benchmark, reflecting the solving speedup of Figure~\ref{fig:bsp-solving}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{hpc/\ThesisGraphQuality/bsp/native_clingo_total.png}
    \caption{One run per point; a curve ends where its variant stops solving within the limits.}
    \label{fig:bsp-total}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{hpc/\ThesisGraphQuality/bsp/native_solving.png}
    \caption{\texttt{la} solving time is close to zero across all sizes, whereas \texttt{ga} degrades sharply at $n=60$ and times out from $n=70$ due to clasp's 16-bit weight saturation ($>32{,}767$).}
    \label{fig:bsp-solving}
\end{figure}

\begin{table}[H]
\centering
\small
\begin{tabular}{lrrrrr}
\toprule
\textbf{Variant} & \textbf{Total (s)} & \textbf{Grounding (s)} & \textbf{Solving (s)} & \textbf{Choices} & \textbf{Conflicts}\\
\midrule
\texttt{gc\_noheur} & 205.1 & 160.9 & 44.1 & $78{,}565$ & $49{,}993$\\
\texttt{gc}        & 458.2 & 167.7 & 290.5 & $231{,}260$ & $160{,}115$\\
\texttt{ga}        & \emph{T} & 172.6 & $>427.3$ & $694{,}660$ & $510{,}480$\\
\texttt{ga\_weak}   & 242.1 & 172.6 & 69.6 & $121{,}159$ & $80{,}847$\\
\texttt{la}        & \textbf{154.2} & \textbf{154.2} & \textbf{0.04} & \textbf{120} & \textbf{0}\\
\texttt{lc}        & 207.2 & 159.7 & 47.5 & $78{,}565$ & $49{,}993$\\
\texttt{la\_co}     & 0.009 & $<0.01$ & 0.01 & 117 & 0\\
\bottomrule
\end{tabular}
\caption{Execution time and solver statistics on BSP at $n=120$. For \texttt{ga}, solving time represents the last value recorded before timing out.}
\label{tab:bsp-n120}
\end{table}

Three points follow:
\begin{enumerate}
    \item In \texttt{la}, default negation and dynamic aggregate evaluation let clingo use the heuristic from the first decision, taking exactly 120 choices with zero conflicts at $n=120$.
    \item \texttt{ga} matches \texttt{la} exactly up to $n=50$, then degrades at $n=60$ and times out from $n=70$ onward. The cause is clasp's 16-bit weight field, as explained in Section~\ref{sec:weight-problem}: once weights saturate beyond $n=50$, the ranking no longer discriminates between candidates.
    \item \texttt{ga\_weak} tracks close to the baseline and is discussed in Section~\ref{sec:ga-weak}, while \texttt{la\_co} is analyzed in Section~\ref{sec:constr-form}.
\end{enumerate}

\section{BSP: the Two Semantics Under the Microscope}
\label{sec:bsp-semantics}

As shown in Figure~\ref{fig:bsp-search}, \texttt{la} requires exactly $n$ decisions with zero conflicts at every benchmarked size $n$. Under Alpha-like semantics, dynamic aggregate evaluation gives the propagator usable guidance from the first decision on an empty assignment, and every choice it proposes lies on the solution path.

In contrast, \texttt{lc} exhibits search statistics that are identical point-by-point to the heuristic-free baseline (\texttt{gc\_noheur}), recording $78{,}565$ choices and $49{,}993$ conflicts at $n=120$. 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/native_choices.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/native_conflicts.png}
    \caption{BSP search statistics on the C++ backend: solver choices (left) and conflicts (right) across sizes. \texttt{la} stays flat at $n$ choices with zero conflicts, while \texttt{lc} overlaps the heuristic-free baseline \texttt{gc\_noheur} point by point.}
    \label{fig:bsp-search}
\end{figure}

Under clingo semantics, aggregate guards on \texttt{c/1} require all elements to be assigned before the sum can be evaluated and an atom is considered \enquote{assigned false} only when clasp demonstrates that it cannot be true. During search, these conditions are never met at a decision point. At $n=120$ the propagator is consulted on every decision and yields zero applicable candidates across the entire run. Because both variants run on the same native C++ backend, the comparison shows that lazy evaluation alone is not sufficient: if the semantics cannot read partial information, the solver falls back on default CDNL branching.

\section{BSP: Rules, Variables, and Memory}

The reduction of the heuristic footprint is also evident in the size of the propositional formula generated for the solver. As shown in Table~\ref{tab:n100-vars}, at $n=100$ the two lazy variants replace the $1{,}010{,}200$ ground directives of \texttt{gc} with the two static facts of the DSL, and their variable count ($20{,}300$) coincides exactly with the heuristic-free baseline: the heuristic leaves no trace in the propositional formula. The ground variants pay this cost in different forms. In \texttt{gc} the directives dominate the formula itself, which grows to more than one million variables, fifty times the baseline; in \texttt{ga} the variable count stays close to the baseline, but the same million directives still have to be grounded, and beyond $n=50$ this effort does not translate into usable guidance. Only \texttt{la\_co} falls below the baseline, with $101$ variables, because it also reformulates the balance constraint (Section~\ref{sec:constr-form}).

\begin{table}[H]
\centering
\small
\begin{tabular}{lrr}
\toprule
\textbf{Variant} & \textbf{Ground Directives / Facts} & \textbf{Solver Variables}\\
\midrule
\texttt{gc}        & $1{,}010{,}200$ & $1{,}030{,}606$\\
\texttt{ga}        & $1{,}010{,}200$ & $20{,}402$\\
\texttt{ga\_weak}   & $1{,}010{,}200$ & $20{,}406$\\
\texttt{lc}        & $2$             & $20{,}300$\\
\texttt{la}        & $2$             & $20{,}300$\\
\texttt{gc\_noheur} & $0$             & $20{,}300$\\
\texttt{la\_co}     & $2$             & $101$\\
\bottomrule
\end{tabular}
\caption{Heuristic grounding footprint and propositional solver variables at $n=100$, C++ backend.}
\label{tab:n100-vars}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{hpc/\ThesisGraphQuality/bsp/native_mem.png}
    \caption{BSP: peak resident set size on the C++ backend across instance sizes.}
    \label{fig:bsp-memory}
\end{figure}

Figure~\ref{fig:bsp-memory} shows that on large instances, memory usage is dominated by the base problem encoding; thus, while lazy heuristics eliminate the memory overhead of heuristic directives, scaling on BSP remains bounded by the ground footprint of the base program, a limitation addressed by constraint reformulation in Section~\ref{sec:constr-form}.

\section{The Price of Laziness: Per-Decision Cost}
\label{sec:per-decision-cost}

The lazy evaluation model represents a space-time trade-off where instead of suffering a combinatorial cost in memory during the grounding phase, there is a cost in computation incurred for each decision made during the search phase. 
There is also a remarkable difference between the two backends; the C++ one manages to be orders of magnitude faster in its decisions.

\begin{table}[H]
\centering
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{ll rr@{\hspace{9pt}} rr@{\hspace{9pt}} rrr}
\toprule
 & & \multicolumn{2}{c}{\textbf{Decide calls}} & \multicolumn{2}{c}{\textbf{Cost (ms/decide)}} & \multicolumn{3}{c}{\textbf{Prolog backend only}} \\
\cmidrule(lr){3-4}\cmidrule(lr){5-6}\cmidrule(lr){7-9}
\textbf{Family} & \textbf{Var.} & \textbf{C++} & \textbf{Prolog} & \textbf{C++} & \textbf{Prolog} & \textbf{cand./call} & \textbf{sync (s)} & \textbf{prop.\ share}\\
\midrule
BSP $n{=}120$ & \texttt{la} & 120        & 120        & $1.0\!\cdot\!10^{-4}$ & 0.62 & 121       & 3.4\,ms  & 0.05\% \\
BSP $n{=}120$ & \texttt{lc} & $78{,}565$ & $78{,}565$ & $1.5\!\cdot\!10^{-4}$ & 0.21 & 0.0       & 3.30\,s  & 8.7\%  \\
PUP $N{=}160$ & \texttt{la} & 217        & 197        & $3.8\!\cdot\!10^{-4}$ & 3.84 & 5.3       & 8.20\,s  & 7.1\%  \\
PUP $N{=}200$ & \texttt{la} & 272        & 249        & $5.6\!\cdot\!10^{-4}$ & 6.74 & 5.2       & 16.53\,s & 7.3\%  \\
HRP $N{=}20$  & \texttt{la} & 122        & 106        & $1.3\!\cdot\!10^{-4}$ & 9.68 & $1{,}248$ & 0.035\,s & 68.3\% \\
HRP $N{=}14$  & \texttt{lc} & $66{,}717$ & $73{,}143$ & $0.9\!\cdot\!10^{-4}$ & 3.44 & 239       & 11.09\,s & 99.4\% \\
\bottomrule
\end{tabular}
\caption{Per-decision overhead on representative solved instances. The last three columns have no C++ counterpart: the Prolog backend re-evaluates the heuristic query from scratch at every decision and mirrors the trail explicitly, whereas the C++ backend keeps its ranked targets incrementally and inspects one entry per call by construction.}
\label{tab:per-decision}
\end{table}

The overall overhead varies dramatically across the benchmark configurations:
\begin{itemize}
    \item When the heuristic has \textbf{perfect guidance (\texttt{la} on BSP)}, with only 120 decide calls taking $0.62$\,ms each in Prolog (and $10^{-4}$\,ms in C++), propagator overhead accounts for less than $0.10\%$ of the total runtime.
    \item When the semantics gets in the way \textbf{(\texttt{lc} on BSP)}, although individual query costs remain low, making many futile calls causes the propagator to consume a significant portion of the total runtime without aiding search.
    \item When candidate generation becomes heavy \textbf{(\texttt{la} on HRP)}, producing over $1{,}200$ candidates per decision, Prolog evaluation time  increases to $9.68$\,ms per call; as a result, the propagator absorbs $68\%$ of the total runtime, even though search needs only about 100 decisions.
    \item When frequent search and expensive evaluation collide \textbf{(\texttt{lc} on HRP)}, making $73{,}143$ futile calls with $239$ candidates each creates a worst-case scenario: Prolog evaluation and trail synchronization eat up to $99.4\%$ of the total runtime, two orders of magnitude above what the same misguided trajectory costs on the C++ backend.
\end{itemize}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/prolog_decide_calls.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/prolog_ms_per_decide.png}
    \caption{BSP, Prolog backend: decide calls and per-decision query cost. The \texttt{lc} curve pairs tens of thousands of calls with a low unit cost; \texttt{la} the opposite.}
    \label{fig:bsp-instrumentation}
\end{figure}

Under Alpha semantics, the query successfully identifies and evaluates all eligible candidate atoms at each step, resulting in a slightly higher query time ($0.62$\,ms per call) due to Prolog term unification and weight calculation. Conversely, under clingo semantics, the negative body condition fails immediately on partial assignments, producing zero candidates; while this allows individual queries to terminate faster ($0.21$\,ms), it provides no guidance whatsoever to the solver, causing a massive explosion in decision calls.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{hpc/\ThesisGraphQuality/bsp/comparison_solving.png}
    \caption{BSP: solving time comparison between the two backends. The bottom subplot shows the relative runtime ratio ($\text{Time}_{\text{Prolog}} / \text{Time}_{\text{C++}}$), where values above 1.0 indicate the factor by which the interpreted Prolog backend is slower than compiled C++.}
    \label{fig:bsp-comparison}
\end{figure}

The total lazy overhead therefore factorizes as:
\[
\text{Total Overhead} = \text{Decide Calls} \times (\text{Query Cost} + \text{Amortized Synchronization Cost}).
\]
Lazy evaluation is thus cheapest exactly when the heuristic works: a richer condition costs more per decision, but a heuristic that guides well leaves few decisions to pay for.

\section{Two Ground Simulations of Alpha: \texttt{ga} versus \texttt{ga\_weak}}
\label{sec:ga-weak}

How closely can Alpha semantics be simulated within a ground-and-solve pipeline? Section~\ref{sec:ga-simulation} introduced two distinct transformations: \texttt{ga\_weak}, which simply removes negative atoms from rule bodies, and \texttt{ga}, which replaces aggregate equality checks with dynamically unrolled monotone bounds ($S \le \#\mathtt{sum}\{Y : c(Y)\}$).

In BSP, both \texttt{ga} and \texttt{ga\_weak} produce the exact same $\Theta(n^3)$ ground directives as the baseline \texttt{gc} ($1{,}120$ directives at $n=10$, $8{,}440$ at $n=20$, and $27{,}960$ at $n=30$). Dropping the \texttt{sum\_value(S)} domain in \texttt{ga\_weak} does not reduce the grounding size: with the equality binding in place, the grounder must still enumerate all subset sums.
The main difference is that equality bindings trigger at most once, and only after all atoms in the sum are assigned, whereas monotone bounds activate incrementally as the partial sum grows.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/expl_gaweak_solving.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/expl_gaweak_choices.png}
    \caption{Solving time (left) and solver choices (right) for BSP comparing the Alpha-like semantics simulations (\texttt{ga} and \texttt{ga\_weak}) against the baseline variants.}
    \label{fig:gaweak}
\end{figure}

Beyond $n=50$, \texttt{ga} deteriorates rapidly as weights exceed the \texttt{int16} range and saturate at $32{,}767$: distinct partial sums collapse onto the same weight, and the ranking stops discriminating between candidate decisions (Figure~\ref{fig:gaweak}). At $n=60$ it already required $1.7$\,million choices and $137$\,s of solving time, before timing out from $n=70$ on.

\texttt{ga\_weak} never hits the timeout, even though its weight expression is the same as \texttt{ga}'s: the equality binding is satisfied only once every element of the sum is assigned, so the heuristic almost never activates and the saturation is never reached. For the same reason it gives no useful guidance. At $n=120$ its search trajectory is worse and noisier than the heuristic-free baseline, taking about $50\%$ more choices and correspondingly longer to solve (Table~\ref{tab:bsp-n120}).

Omitting the negative guards without unrolling the aggregate bounds therefore yields no usable guidance: it increases search effort and slightly reduces the solvable horizon.

\section{Evaluating Pre-Materialized Auxiliary Predicates}
\label{sec:aux-form}

The \texttt{la\_aux} variant investigates whether the primary advantage of lazy evaluation stems simply from reducing the number of \texttt{\_\_heuristic} directives, or from keeping the evaluation of heuristic conditions entirely outside the ground program. In \texttt{la\_aux}, heuristic conditions and aggregate values are pre-materialized into auxiliary ASP predicates (\texttt{h\_b(X,S)} and \texttt{h\_c(X,S)}), which the lazy propagator then queries during search.

Defining auxiliary rules forces the grounder to instantiate the full Cartesian product of elements and partial sums $(X, S)$. As a consequence, solver variables increase to $132{,}756$ at $n=50$, compared to just $5{,}150$ in direct \texttt{la}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/expl_laaux_solving.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/expl_laaux_decide_calls.png}
    \caption{Solving time on the C++ backend (left) and number of propagator decide calls on the Prolog backend (right) comparing \texttt{la\_aux} against direct \texttt{la} in BSP.}
    \label{fig:laaux}
\end{figure}

The auxiliary materialization costs performance on both backends (Figure~\ref{fig:laaux}):
\begin{itemize}
    \item Because the propagator must scan all materialized auxiliary candidates, the per-decision cost scales as $\mathcal{O}(n^2)$. Solving time diverges sharply from $n=40$, reaching $287.1$\,s at $n=60$ before consistently timing out from $n=70$ onward. In contrast, the direct \texttt{la} formulation maintains negligible solving times across the entire range.
    \item In the Prolog backend, the sheer volume of auxiliary candidates multiplies the required consultations, jumping from $573$ calls at $n=20$ to $8{,}452$ at $n=30$ (compared to exactly $n$ calls in direct \texttt{la}). Trail synchronization alone accounts for $99.2\%$ of the total runtime ($383$\,s out of $386$\,s) at $n=30$, causing the backend to time out for all sizes $n \ge 40$.
\end{itemize}

Reducing the number of heuristic directives is therefore not enough on its own: the benefit comes from keeping the evaluation of the heuristic conditions out of the ground program. Once those conditions are materialized as auxiliary rules, the combinatorial cost simply moves from the directives to the base encoding, and the lazy propagator inherits it at every decision.

\section{Effect of the Constraint Formulation}
\label{sec:constr-form}

The \texttt{la\_co} variant replaces the quadratic difference check over the two sums with a single constant-bound interval constraint, substantially simplifying the grounding structure.

In the standard formulations (\texttt{gc}, \texttt{la}, and \texttt{lc}), this quadratic constraint dominates the grounding phase: at $n=120$ it produces about $52.8$\,million propositional rules and required between two and three minutes of grounding. Direct lazy evaluation (\texttt{la}) removes the heuristic directives, but the ground program remains bound by the size of the domain specification.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/expl_laco_grounding.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/expl_laco_rules.png}
    \caption{Grounding time (left) and propositional rule count (right) for BSP on the C++ backend, comparing the compact constraint formulation (\texttt{la\_co}) against the baseline encodings (\texttt{gc}, \texttt{la}, and \texttt{lc}).}
    \label{fig:laco}
    \vspace{-1em}
\end{figure}

The reformulation changes formula size and grounding time by orders of magnitude (Figure~\ref{fig:laco}):
\begin{itemize}
    \item \texttt{gc}, \texttt{la}, and \texttt{lc} overlap near the top of both plots, sharing the same quadratic base encoding, whereas \texttt{la\_co} flattens along the horizontal axis. At $n=120$ the propositional formula drops from $52.8$\,million rules to $366$.
    \item Grounding all but disappears. Total clingo runtime at $n=120$ falls from $154.2$\,s in \texttt{la} to $9$\,ms, with the search guidance unchanged ($117$ choices, zero conflicts). The same holds up to $n=200$, which solves in $14$\,ms.
\end{itemize}

The two bottlenecks are therefore independent. Once lazy heuristics remove the search cost, what remains in BSP is set entirely by the materialization of the domain constraints, and pairing lazy heuristics with a compact encoding addresses both at once.

\section{PUP: the Grounding Bottleneck at Scale}
\label{sec:pup}

The Partner Units Problem (PUP) is an industrial configuration benchmark on which domain-specific heuristics keep the combinatorial search under control. Unlike BSP, which uses only \texttt{\#sum}, PUP combines dynamic \texttt{\#count} and \texttt{\#max} aggregates, the latter tracking the remaining capacity across adjacent partner units.

In BSP, unguided search dominated baseline execution times. In PUP, the dynamic Alpha-like semantics reduces solving effort to a few hundred decisions at every instance size, so more than $95\%$ of the total execution time moves to grounding and the cost becomes one of memory. PUP therefore measures the grounding and memory efficiency of lazy evaluation at scale.

Figure~\ref{fig:pup-total} and Table~\ref{tab:pup-frontier} summarize the overall performance across all five variants under a $600$\,s timeout and a $30$\,GB memory limit.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{hpc/\ThesisGraphQuality/pup/native_clingo_total.png}
    \caption{Total clingo runtime on PUP across instance sizes for the C++ backend (curves terminate at the largest solved instance).}
    \label{fig:pup-total}
\end{figure}

\begin{table}[H]
\centering
\footnotesize
\begin{tabular}{lrrrrr}
\toprule
\textbf{Variant} & \textbf{max $N$ solved} & \textbf{Total (s)} & \textbf{Grounding (s)} & \textbf{Peak RSS} & \textbf{First failure}\\
\midrule
\texttt{gc\_noheur} & 80  & 123.2  & 10.6  & 0.7 GB & T@100\\
\texttt{gc}        & 60  & 97.0   & 17.7  & 1.3 GB & T@80\\
\texttt{ga}        & 200 & 445.1  & 436.6 & 24.6 GB & ---\\
\texttt{la}        & 200 & 231.4  & 225.8 & 12.7 GB & ---\\
\texttt{lc}        & 100 & 241.5  & 27.0  & 2.1 GB & T@80 (non-mon.)\\
\bottomrule
\end{tabular}
\caption{PUP frontier on the C++ backend ($600$\,s / $30$\,GB budget): largest solved \texttt{double-$N$} instance and corresponding resource metrics.}
\label{tab:pup-frontier}
\end{table}

\paragraph{Analysis of baseline failures.}
The baseline results reveal two distinct failure dynamics:
\begin{itemize}
    \item In \texttt{gc\_noheur}, the lack of heuristic guidance forces the solver to rely entirely on VSIDS. Combinatorial choices escalate rapidly ($546{,}549$ choices at $N=80$), leading to a timeout at $N=100$.
    \item In \texttt{gc}, the standard ground heuristic does worse than no heuristic at all, failing earlier at $N=80$ with $865{,}136$ choices. Because clingo's semantics requires aggregate bodies to be fully assigned before a directive is evaluated, the heuristic rules never activate during search. The inert ground directives nonetheless add variables to the solver, enlarging the search space and degrading VSIDS branching.
\end{itemize}

\paragraph{Grounding and memory savings (\texttt{la} vs \texttt{ga}).}
Both Alpha-like semantics variants (\texttt{ga} and \texttt{la}) solve the entire benchmark horizon up to $N=200$, maintaining near-perfect search guidance ($235$ choices for \texttt{ga} vs $162$ for \texttt{la} at $N=120$). Because solving takes only a few seconds, the overall performance is differentiated almost exclusively by the grounding footprint.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{hpc/\ThesisGraphQuality/pup/native_grounding.png}
    \caption{Grounding time on PUP for the C++ backend across instance sizes.}
    \label{fig:pup-grounding}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{hpc/\ThesisGraphQuality/pup/native_mem.png}
    \caption{Peak memory usage on PUP for the C++ backend, illustrating the $2\times$ memory reduction of \texttt{la} over \texttt{ga}.}
    \label{fig:pup-mem}
\end{figure}

As shown in Figures~\ref{fig:pup-grounding} and~\ref{fig:pup-mem}, \texttt{ga} must unroll all discrete bounds of the dynamic aggregates into the ground program. At $N=200$ this costs $436.6$\,s of grounding and $24.6$\,GB of memory, against a $30$\,GB limit. Lazy evaluation in \texttt{la} leaves the aggregates to the in-memory C++ propagator and grounds only the base problem specification, reducing grounding to $225.8$\,s and peak memory to $12.7$\,GB. The saving holds across the whole range: the ratio between the two variants is largest on the small instances and settles just below a factor of two at $N=200$, in both time and memory.

\paragraph{Controlled semantic comparison (\texttt{la} vs \texttt{lc}).}
In PUP, the lazy propagator requires auxiliary relations to expose aggregate sources. As a result, the ground propositional programs for \texttt{la} and \texttt{lc} are completely identical ($216{,}193$ rules at $N=20$), differing exclusively in the runtime evaluation semantics configured within the propagator:
\begin{itemize}
    \item \texttt{la} evaluates dynamic aggregates over currently true atoms, solving $N=120$ in $47.9$\,s with only $162$ choices.
    \item \texttt{lc} waits for complete assignments, generating zero active candidates during search. At $N=120$, it makes $53{,}036$ choices and times out.
\end{itemize}
The unguided nature of \texttt{lc} also explains its non-monotonic failure pattern in Table~\ref{tab:pup-frontier} (timing out on \texttt{double-80} but solving \texttt{double-100}). Without heuristic guidance, the solver is subject to the high variance of CDNL restarts, occasionally finding an early solution by chance before failing at larger sizes.

For \texttt{la}, both backends deliver guidance of the same quality, holding the search down to a few hundred choices on either engine.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{hpc/\ThesisGraphQuality/pup/comparison_solving.png}
    \caption{Solving time (top) and Prolog-to-C++ runtime ratio (bottom) on PUP across execution backends.}
    \label{fig:pup-comparison-solving}
\end{figure}

As shown in Figure~\ref{fig:pup-comparison-solving}, solving time remains under $25$\,s across the whole horizon on both backends, with the Prolog runtime ratio remaining stable around $4\times$ the native C++ engine for \texttt{la}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.92\textwidth]{hpc/\ThesisGraphQuality/pup/prolog_state_sync.png}
    \caption{Cumulative state synchronization time for the Prolog backend on PUP.}
    \label{fig:pup-state-sync}
\end{figure}

However, as illustrated in Figure~\ref{fig:pup-state-sync}, in the Prolog backend, synchronizing the solver trail accounts for $16.5$\,s at $N=200$, whereas actual Prolog query evaluation takes only $1.67$\,s. In comparison, the native C++ backend updates its in-process aggregate structures directly along the solver trail, completing all synchronization in just $0.68$\,s.

\section{HRP: Semantics Without Aggregates}
\label{sec:hrp}

HRP evaluates heuristic semantics on rules with nested default negations but without dynamic aggregates. Because HRP instances are relatively small, nearly all variants solve every instance in under a second on the C++ backend, so the family measures guidance quality and per-decision overhead rather than reach.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/hrp/native_choices.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/hrp/comparison_solving.png}
    \caption{HRP: solver choices on the C++ backend (left) and the C++ versus Prolog solving comparison (right). The only diverging curve is \texttt{lc}, in both plots.}
    \label{fig:hrp}
\end{figure}

\begin{itemize}
    \item under \textbf{Alpha-like semantics:} \texttt{la} tracks \texttt{ga} closely at every size ($86$ vs $88$ choices at $N=14$, with zero conflicts for both), confirming that lazy evaluation faithfully replicates reference Alpha guidance.
    \item under \textbf{clingo-like semantics:} the reading of negation delays the higher-level heuristic rules while letting incomplete lower-level rules fire, which misdirects the search. \texttt{lc} required $66{,}717$ choices at $N=14$ (an 800-fold increase over \texttt{la}) and $14.2$ million choices at $N=16$, timing out from $N=18$.
\end{itemize}

Beyond guidance quality, HRP also illustrates the operational cost of relational candidate matching in the Prolog backend. Because the multi-room placement rules join multiple domain predicates, the SWI-Prolog engine must evaluate over $1{,}200$ candidate pairs per decision at $N=20$, driving unit query cost to $9.68$\,ms (Table~\ref{tab:per-decision}). 

For \texttt{la}, where search is resolved in only $106$ choices, this per-decision effort accounts for $68.3\%$ of the total runtime ($1.06$\,s out of $1.55$\,s), compared to just $0.38$\,s on the compiled C++ backend. For \texttt{lc}, the combination of misdirected search and relational query overhead creates a catastrophic bottleneck: making $73{,}143$ queries, already at $N=14$, inflates total Prolog execution time to $264.5$\,s (with $99.4\%$ spent inside the propagator), whereas the compiled native backend resolves the same misguided trajectory in $1.80$\,s.

\section{An External Reference: Alpha}
\label{sec:alpha}

To evaluate how closely our propagator reproduces the lazy evaluation process and performance advantages, we compare our clingo variants against Alpha with and without its declarative heuristic engine (Qh mode). 

On BSP, the guided Alpha configuration (\texttt{alpha\_qh}) solves all twenty instances in $3.4$\,s and $0.37$\,GB at $n=200$, requiring exactly $n$ decisions and zero backtracks. This matches the search trajectory achieved by \texttt{la} in clingo ($n$ choices, zero conflicts). The performance gap at larger sizes ($586.5$\,s for \texttt{la} at $n=160$ versus $2.4$\,s for \texttt{alpha\_qh}, both on the \texttt{runlim} clock) stems entirely from base program grounding in gringo ($52.8$ million rules at $n=120$), which Alpha completely avoids by instantiating rules lazily along the search trail.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{hpc/\ThesisGraphQuality/alpha/bsp_dashboard.png}
    \vspace{0.2em}
    \caption{BSP benchmark dashboard: the four clingo variants and the heuristic-free baseline \texttt{gc\_noheur} compared against Alpha with (\texttt{alpha\_qh}, labeled \emph{Alpha Qh}) and without (\texttt{alpha\_noheur}, labeled \emph{Alpha (no heur)}) declarative heuristics. Left: total wall-clock time; right: peak RSS. While \texttt{alpha\_qh} stays flat along the horizontal axis, unguided \texttt{alpha\_noheur} degrades rapidly, failing earlier than any clingo variant.}
    \label{fig:alpha-bsp}
\end{figure}

Lazy grounding alone is not sufficient without dynamic heuristic guidance: \texttt{alpha\_noheur} solves nothing beyond $n=40$, eleven sizes short of clingo's ground baseline \texttt{gc\_noheur} ($n=150$). The two systems sit on opposite sides of one trade-off:
\begin{itemize}
    \item \textbf{clingo:} Because the program is fully grounded before search begins, exploring unpromising search branches is computationally very cheap: clasp only has to perform fast boolean propagation and VSIDS variable scoring over pre-existing clauses in compiled C++.
    \item \textbf{Alpha:} Because rules are grounded on demand during search, exploring unpromising branches is extremely expensive: every wrong decision and subsequent backtrack forces the engine to instantiate new rules and nogoods on the fly.
\end{itemize}

As shown in Figure~\ref{fig:alpha-internals}, \texttt{alpha\_noheur} suffers a severe search space explosion, peaking at over $67{,}000$ guesses and backtracks at $n=30$; it still solves $n=40$, where each guess costs more even though fewer are needed, and from $n=50$ it no longer finishes at all. With heuristic guidance, \texttt{alpha\_qh} never backtracks and materializes only the minimal subset of ground rules along the single valid solution path. The two mechanisms are therefore complementary: dynamic heuristics need lazy evaluation to avoid the grounding bottleneck, and lazy grounding needs dynamic heuristics to avoid paying instantiation cost on every wrong branch.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{hpc/\ThesisGraphQuality/alpha/bsp_internals.png}
    \vspace{0.2em}
    \caption{BSP internal search counters for Alpha across instance sizes. Top row: with heuristics (\texttt{alpha\_qh}), guesses remain equal to $n$ with zero backtracks; without heuristics (\texttt{alpha\_noheur}), search explodes past $n=20$ and the curve stops at $n=40$, the last size it solves. Bottom row: cumulative Prolog query evaluation time for \texttt{alpha\_qh} ($2.2$\,s total at $n=200$, averaging $1.0$ to $5.4$\,ms per consultation).}
    \label{fig:alpha-internals}
\end{figure}

\paragraph{Partner Units Problem (PUP).}
On PUP, clingo with lazy heuristics (\texttt{la}) closely approaches Alpha's performance, solving $N=200$ in $234.7$\,s and $12.7$\,GB compared to $50.8$\,s and $10.6$\,GB for \texttt{alpha\_qh} (a factor of $4.6\times$ in time and $1.2\times$ in memory). In contrast, the static ground simulation \texttt{ga} required $451.4$\,s and $24.6$\,GB (nine times the runtime and over twice the memory of \texttt{alpha\_qh}). Furthermore, \texttt{alpha\_noheur} solves no instances on PUP, reaffirming that dynamic heuristics are essential for solving this problem family.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{hpc/\ThesisGraphQuality/alpha/pup_dashboard.png}
    \vspace{0.2em}
    \caption{PUP benchmark dashboard: external comparison between clingo variants and Alpha. The baseline \texttt{alpha\_noheur} does not appear as it solves zero instances. The lazy clingo variant \texttt{la} remains within a small factor of \texttt{alpha\_qh} in both time and memory, whereas the ground simulation \texttt{ga} incurs substantial overhead on both axes.}
    \label{fig:alpha-pup}
\end{figure}

\paragraph{Overall Frontier and Query Engines.}
Table~\ref{tab:alpha-frontier} summarizes the maximal instance reach and resource consumption across systems. Regarding query engines, Alpha's Qh mode and the Prolog backend of this thesis both evaluate heuristics by querying an embedded Prolog engine over partial assignments. Consultation costs are of similar magnitude: at BSP $n=120$, Alpha issues $242$ queries at $2.87$\,ms each, against $120$ consultations at $0.62$\,ms for our Prolog backend. The synchronization-based design is therefore competitive with the reference engine on consultation cost.

\begin{table}[H]
\centering
\small
\begin{tabular}{llrrr}
\toprule
\textbf{Family} & \textbf{System} & \textbf{max solved} & \textbf{Wall (s)} & \textbf{Peak RSS}\\
\midrule
BSP & \texttt{alpha\_noheur}            & 40  & 30.5  & 1.1 GB\\
BSP & clingo \texttt{gc\_noheur}        & 150 & 563.6 & 15.5 GB\\
BSP & clingo \texttt{la}               & 160 & 586.5 & 19.8 GB\\
BSP & \texttt{alpha\_qh}               & 200 & 3.4   & 0.37 GB\\
BSP & clingo \texttt{la\_co}            & 200 & 0.04  & $<0.1$ GB\\
\midrule
PUP & \texttt{alpha\_noheur}            & --- & ---   & ---\\
PUP & clingo \texttt{gc\_noheur}        & 80  & 123.3 & 0.7 GB\\
PUP & clingo \texttt{lc}               & 100 & 242.2 & 2.1 GB\\
PUP & clingo \texttt{ga}               & 200 & 451.4 & 24.6 GB\\
PUP & clingo \texttt{la}               & 200 & 234.7 & 12.7 GB\\
PUP & \texttt{alpha\_qh}               & 200 & 50.8  & 10.6 GB\\
\bottomrule
\end{tabular}
\caption{Largest instance solved within the 600\,s / 30\,GB budget, and its cost, ordered by reach within each family. Wall time and peak RSS are \texttt{runlim} external process measures; Alpha's RSS includes the reserved JVM heap. clingo rows represent the native C++ backend. A dash marks a configuration that solved no instances.}
\label{tab:alpha-frontier}
\end{table}

\section{C++ versus Prolog: Backend Comparison}
\label{sec:backend-summary}

Across the campaign the compiled C++ and interpreted Prolog backends behave consistently, producing identical solvability frontiers in all but five configurations. Whenever heuristic consultations are infrequent or cheap, as in \texttt{la} on PUP and HRP, both engines reach the same instance size.

Where the two backends diverge, the Prolog engine consistently fails earlier due to evaluation and synchronization overhead. On BSP, the frontier drops by a single step for \texttt{la} ($n=160 \to 150$) and \texttt{lc} ($150 \to 140$) because both runs finish within seconds of the 600-second time limit. On PUP ($N=100 \to 40$) and HRP ($N=16 \to 14$) under \texttt{lc}, the unguided search triggers tens of thousands of futile queries, turning Prolog's per-call interpretation cost into the dominant bottleneck.

The C++ backend evaluates heuristic decisions three to five orders of magnitude faster than the interpreted Prolog engine ($\approx 10^{-4}$\,ms against $0.2$--$9.7$\,ms per call). The native backend is therefore the faster of the two, and the Prolog backend the more expressive: its rule bodies are ordinary logic programs, which makes prototyping easier.

\section{Summary of Results}
\label{sec:results-summary}

Moving dynamic aggregate evaluation from grounding to search reduces the heuristic representation on BSP from $\Theta(n^3)$ ground directives to two constant template facts. On PUP this halves both grounding time ($225.8$\,s against $436.6$\,s) and peak memory ($12.7$\,GB against $24.6$\,GB) with respect to the static ground simulation \texttt{ga}. The exploratory variants show that the benefit depends on keeping the whole evaluation dynamic: omitting negative guards without unrolling the aggregates (\texttt{ga\_weak}) gives no usable guidance, and pre-materializing heuristic bodies into auxiliary ASP rules (\texttt{la\_aux}) puts the combinatorial explosion back into the base program. Pairing lazy heuristics with compact domain constraints (\texttt{la\_co}) removes the grounding and the solving bottleneck together, solving BSP up to $n=200$ in milliseconds.

The evaluation semantics decides whether the guidance works at all. Under Alpha-like semantics, dynamic aggregate evaluation yields usable advice on partial assignments: $n$ choices with zero conflicts on BSP, and the intended multi-level ranking on HRP. Standard clingo semantics requires complete assignments before an aggregate body can be evaluated, which leaves the heuristic inert on BSP and misleading on HRP, where the delayed high-level rules let incomplete lower-level rules steer the solver into far larger subspaces. Static ground unrolling (\texttt{ga}) approximates dynamic aggregates on small instances, but it slows grounding and fails once the flattened weights exceed clasp's 16-bit ceiling ($32{,}767$) --- a limit the lazy propagator never meets, since it ranks its decisions internally.

Lazy evaluation trades grounding memory for decision-time computation, and the total overhead is the number of decisions times the cost of a consultation. The mechanism is therefore cheapest exactly when the heuristic guides well, because the solver then makes few decisions. Against the reference lazy solver Alpha, the propagator reproduces the reference search trajectories and stays within a factor of $4.6\times$ in execution time and $1.2\times$ in peak memory on PUP, without the static unrolling cost of the ground-and-solve variants.
