\chapter{Results}
\label{sec:results}

\section{Functional Validation}
\label{sec:validation}

The system was validated by keeping three levels separate. The first level concerns the ASP program itself: the answer sets, the \mintinline{text}|SAT|/\mintinline{text}|UNSAT| result, and the visible solutions must remain consistent across the compared encodings and across the two backends. The second level concerns the heuristic translation: targets, bodies, weights, modifiers, and aggregate bindings must be preserved when a native \mintinline{text}|#heuristic| directive is rewritten as a lazy fact, including the flattening of levels into the weight, which the suite applies uniformly to every variant and to both backends. The third level concerns the effect on search, measured through choices, conflicts, solving time, memory, and the size of the ground heuristic component.

Heuristics do not change the semantics of the ASP program: they guide search, but they do not add or remove answer sets. The equivalence harness \path{tools/check_equivalence.py}, run by the sanity entry point ahead of every campaign, verifies this property exhaustively on BSP, by enumerating all models of every variant on both backends and comparing the collections after projection on \mintinline{text}|b/1| and \mintinline{text}|c/1|, and by SAT/UNSAT agreement on PUP, where exhaustive enumeration is intractable. The same harness enforces the anti-fallback guards for the Prolog backend (\mintinline{text}|decide_calls > 0| for every lazy Prolog run, and equal choice counts between native and Prolog \mintinline{text}|la| on the control instance). On the native side, the C++ test suite isolates the correctness of the heuristic machinery from the benchmarks: it covers incremental aggregate states, empty and unsatisfiable corner cases, explicit body and target bindings, filtered aggregates, the per-channel resolution of the modifiers by weight under both semantics, and the parser's rejection of malformed syntax.

\section{Experimental Setup and Dataset}
\label{sec:dataset}

The campaign reported in this section was executed on an HPC cluster under SLURM, with one job per (system, setting, instance) triple orchestrated by \mintinline{text}|benchmark-tool|. Each job reserved a compute node exclusively and requested 4 cores, and every run was limited by \mintinline{text}|runlim| to 600 seconds of real time and 30~GB of resident memory. The measurement partition holds ten nodes that SLURM reports as identical in processor, sockets, cores, threads and memory; the campaign was served by nine of them, and Section~\ref{sec:time-reliability} measures how much that choice can perturb a time. Both backends were compiled on the compute nodes against the same toolchain, and the Prolog backend embeds a modern SWI-Prolog (10.0.2) built in user space for the same environment.

The dataset covers the three families with their canonical instance ranges: BSP with $n = 10$ to $200$ in steps of $10$; PUP with the \mintinline{text}|double-$N$| instances, $N = 20$ to $200$ in steps of $20$ communication units; HRP with the reconfiguration instances \mintinline{text}|house-$N$|, $N = 2$ to $20$ persons in steps of $2$. All five shared settings run on all families; the three methodological extras (\mintinline{text}|ga_weak|, \mintinline{text}|la_aux|, \mintinline{text}|la_co|) are BSP-specific. The matrix totals 520 runs (320 for BSP, 100 each for PUP and HRP). The external reference system of Section~\ref{sec:alpha-setup} adds 90 more --- \mintinline{text}|alpha| and \mintinline{text}|alpha_noheur| on all three families, plus the HRP-only \mintinline{text}|alpha_dom| --- for 610 runs in the campaign.

No run ended in error: every unsolved run is an explicit timeout (\emph{T}) or memout (\emph{M}). Memouts are rare and concentrated: 10 of the 610, all on BSP and all at $n=180$, five variants on each backend (\mintinline{text}|gc_noheur|, \mintinline{text}|ga|, \mintinline{text}|ga_weak|, \mintinline{text}|la|, \mintinline{text}|lc|), the size at which the base program's own growth reaches the memory ceiling. All ten also hit the time limit, so the two flags rise together and the memout adds no separation. Above $n=180$ the picture changes shape: at $n=190$ and $n=200$ every matrix variant except \mintinline{text}|la_co| stops after $330$ to $405$ seconds without reaching the time limit, because clingo exhausts the memory budget during grounding and reports \mintinline{text}|UNKNOWN|; the runs are recorded as timeouts, and at those two sizes the \emph{T} flag should be read as "did not finish" rather than as a statement about which resource ran out. The dominant failure mode over the campaign as a whole is the time limit, and the frontiers reported below are time-bound except where stated.

The figures of this chapter follow the same separation. Every comparative plot shows the reference encoding and the four variants under study, \mintinline{text}|gc_noheur|, \mintinline{text}|gc|, \mintinline{text}|ga|, \mintinline{text}|la|, and \mintinline{text}|lc|; the three exploratory settings are kept out of these plots on purpose, because each of them answers a different, narrower question, and appear only in the dedicated analyses of Sections~\ref{sec:ga-weak}, \ref{sec:aux-form}, and~\ref{sec:constr-form}, in figures restricted to the variants that each comparison actually needs.

Three properties anchor the validity of the numbers, and one qualifies them. First, within each system the ground variants (\mintinline{text}|g*|) execute identical binaries on identical ground programs, and clasp's search is deterministic at fixed configuration, so choice and conflict counts are exact structural measures rather than noisy samples; they reproduce bit-for-bit the counts observed in the preliminary local campaign (\mintinline{text}|gc| at $n=100$ performs $32{,}514$ choices on both machines). Second, the anti-fallback guard confirmed \mintinline{text}|decide_calls > 0| for every solved lazy Prolog run, so no lazy measurement silently degenerated to the baseline. Third, the two backends agree on the search trajectory wherever the heuristic is total and its ranking admits no ties: on BSP the choice and conflict counts of native and Prolog coincide to the unit in all 100 cells that both backends solve, ground and lazy alike. Where ties occur the agreement is close but not exact. On PUP and HRP the lazy variants differ by a few percent in choices ($217$ against $197$ for \mintinline{text}|la| at PUP $N=160$), because the deterministic tie-break of the two implementations, a literal ordering in C++ and a sort key in Prolog, resolves equal-rank targets differently. Every ground cell of every family remains bit-identical across backends.

\subsection{What the Times Carry}
\label{sec:time-reliability}

The qualification concerns \emph{time}. SLURM dispatches each job to whichever node of the measurement partition is free, so a wall-clock figure could in principle say as much about the machine as about the variant. The campaign measures how much, from its own data, without a separate calibration experiment.

The instrument is the ground half of the matrix. For every ground variant the two backends execute byte-identical encodings on the same solver, so a pair of runs differing only in backend does the same grounding work; when SLURM happens to place the two runs on different nodes, the difference between their grounding times is a measurement of the machines and of nothing else. The campaign contains 46 such pairs across BSP and PUP. Their grounding times differ by a median of $2.2\%$ and by at most $6.6\%$, the largest single disagreement being $6.6\%$ on \mintinline{text}|ga| at $n=60$, where the quantity measured is eight seconds. At the other end of the range the agreement is closer than the reporting precision suggests: \mintinline{text}|gc_noheur| at $n=150$ grounds in $426.2$ seconds on one node and $427.9$ on another, a difference of $0.4\%$ on a seven-minute measurement.

A second, independent check runs inside a single backend. The variants \mintinline{text}|gc_noheur|, \mintinline{text}|la| and \mintinline{text}|lc| share the base encoding verbatim and ground the same program to the rule (at $n=120$, $52{,}759{,}256$ rules against $52{,}759{,}258$, the two extra being the lazy facts themselves). Their native grounding times are $160.9$, $154.2$ and $159.7$ seconds at $n=120$, a spread of $4.4\%$, and $330.6$, $331.5$ and $333.2$ seconds at $n=140$, a spread of $0.8\%$. The two estimates agree on the order of magnitude, so the campaign's time figures are reproducible to a few percent and this chapter reads them as measurements rather than as indications.

One consequence still needs stating. A few percent decides nothing except where a run lands within a few percent of the 600-second limit, and two cells of the campaign do: native \mintinline{text}|la| finishes $n=160$ in $585.7$ seconds and native \mintinline{text}|lc| finishes $n=150$ in $595.0$, both inside a $2.5\%$ margin. Those two frontiers are reported as measured and no argument is built on them. Everywhere else the separations discussed below are between $20\%$ and four orders of magnitude, far above the resolution of the instrument.

Peak RSS does not depend on the machine at all, and the same three same-encoding variants make the point: $6{,}739$, $6{,}743$ and $6{,}745$~MB at $n=120$, a spread of $0.1\%$. Structural measures --- choices, conflicts, rules, variables, atoms, ground objects, decide calls, candidates --- are exact, since clasp's search is deterministic at fixed configuration, and they carry every claim in this chapter that does not need a clock.

Tables~\ref{tab:overview-bsp}--\ref{tab:overview-hrp} summarize the outcome of every cell of the experimental matrix. The columns report how many instances were solved and where the first failure occurs, distinguishing timeouts from memouts.

\begin{table}[H]
\centering
\small
\begin{tabular}{lllll}
\toprule
 & \multicolumn{2}{c}{\textbf{native}} & \multicolumn{2}{c}{\textbf{Prolog}}\\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
\textbf{Variant} & \textbf{solved} & \textbf{first failure} & \textbf{solved} & \textbf{first failure}\\
\midrule
\mintinline{text}|gc_noheur| & 15/20 & T@160 & 15/20 & T@160\\
\mintinline{text}|gc|        & 13/20 & T@140 & 13/20 & T@140\\
\mintinline{text}|ga|        & 6/20  & T@70  & 6/20  & T@70\\
\mintinline{text}|ga_weak|   & 14/20 & T@150 & 14/20 & T@150\\
\mintinline{text}|la|        & 16/20 & T@170 & 15/20 & T@160\\
\mintinline{text}|lc|        & 15/20 & T@160 & 14/20 & T@150\\
\mintinline{text}|la_aux|    & 6/20  & T@70 & 3/20 & T@40\\
\mintinline{text}|la_co|     & 20/20 & --- & 20/20 & ---\\
\bottomrule
\end{tabular}
\caption{BSP campaign overview ($n=10..200$, 600\,s / 30\,GB per run). Every cell is monotone in $n$. At $n=180$ five of the eight variants fail with both flags set (\emph{T} and \emph{M} simultaneously) on both backends, the size at which the base program's own growth exhausts the memory budget at about the point where it would also time out. The two backends agree on six of the eight frontiers; where they differ, the Prolog backend is the one that stops earlier, by one size for \texttt{la} and \texttt{lc} and by three for \texttt{la\_aux}. The \texttt{la} and \texttt{lc} cells are the two the campaign cannot separate cleanly, because the native runs that decide them finish at $585.7$ and $595.0$ seconds against a $600$-second limit (Section~\ref{sec:time-reliability}); the \texttt{la\_aux} gap is a real per-decision cost, analysed in Section~\ref{sec:aux-form}. The separations that no measurement margin can touch are \texttt{ga} and \texttt{la\_aux} at one end and \texttt{la\_co} at the other.}
\label{tab:overview-bsp}
\end{table}

\begin{table}[H]
\centering
\small
\begin{tabular}{lllll}
\toprule
 & \multicolumn{2}{c}{\textbf{native}} & \multicolumn{2}{c}{\textbf{Prolog}}\\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
\textbf{Variant} & \textbf{solved} & \textbf{first failure} & \textbf{solved} & \textbf{first failure}\\
\midrule
\mintinline{text}|gc_noheur| & 4/10  & T@100 & 4/10  & T@100\\
\mintinline{text}|gc|        & 3/10  & T@80 & 3/10  & T@80\\
\mintinline{text}|ga|        & 10/10 & --- & 10/10 & ---\\
\mintinline{text}|la|        & 10/10 & --- & 10/10 & ---\\
\mintinline{text}|lc|        & 4/10  & T@80 (non-mon.) & 2/10 & T@60\\
\bottomrule
\end{tabular}
\caption{PUP campaign overview (\texttt{double-$N$}, $N=20..200$, 600\,s / 30\,GB per run). Under this budget neither \texttt{ga} nor \texttt{la} fails anywhere in the tested range; the two variants whose heuristic carries no dynamic aggregate (\texttt{gc\_noheur}, \texttt{gc}) stall by timeout well inside it. One native cell is not monotone in $N$: \texttt{lc} fails on \texttt{double-80} but solves \texttt{double-100}. This is a property of the family --- one instance per size, with neighbouring sizes of very different hardness --- and Section~\ref{sec:pup} reads it through the choice counts rather than the times.}
\label{tab:overview-pup}
\end{table}

\begin{table}[H]
\centering
\small
\begin{tabular}{lllll}
\toprule
 & \multicolumn{2}{c}{\textbf{native}} & \multicolumn{2}{c}{\textbf{Prolog}}\\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
\textbf{Variant} & \textbf{solved} & \textbf{first failure} & \textbf{solved} & \textbf{first failure}\\
\midrule
\mintinline{text}|gc_noheur| & 10/10 & --- & 10/10 & ---\\
\mintinline{text}|gc|        & 10/10 & --- & 10/10 & ---\\
\mintinline{text}|ga|        & 10/10 & --- & 10/10 & ---\\
\mintinline{text}|la|        & 10/10 & --- & 10/10 & ---\\
\mintinline{text}|lc|        & 8/10  & T@18 & 7/10 & T@16\\
\bottomrule
\end{tabular}
\caption{HRP campaign overview (\texttt{house-$N$}, $N=2..20$ persons, 600\,s / 30\,GB per run).}
\label{tab:overview-hrp}
\end{table}

Three headline facts are visible at this granularity and are unpacked in the following subsections. The first is that the three families put the frontier in three different places, and only two of them are informative. On PUP the frontier separates the variants by more than a factor of two in reach and is set by the heuristic's own grounding, which makes it the family where the comparison is decided. On HRP no variant except \mintinline{text}|lc| is ever in difficulty, so the frontier says nothing and the guidance quality says everything. On BSP the frontier of every same-encoding variant is set by the grounding of the base program, which the heuristic representation does not touch: \mintinline{text}|gc_noheur| stops after $n=150$ on both backends, \mintinline{text}|la| after $160$ and $150$, \mintinline{text}|lc| after $150$ and $140$, a band two sizes wide. What separates them there is not reach but cost: \mintinline{text}|la| arrives at that wall having performed $n$ choices and zero conflicts at every size, with the search cost removed rather than reduced.

The second fact is that two variants separate themselves from that band by margins no measurement can blur, in opposite directions. The ground Alpha simulation \mintinline{text}|ga| collapses on BSP because of the static unrolling that makes it faithful, failing from $n=70$, nine sizes before the heuristic-free baseline; \mintinline{text}|la_co| solves the entire range in milliseconds. Third, the clingo-like lazy variant \mintinline{text}|lc| shows two distinct failure modes, inertness on BSP and active misguidance on HRP, both of which the per-decision instrumentation measures directly. It is also the variant whose frontier moves most between the two backends on the families where the propagator is consulted tens of thousands of times (PUP and HRP), since it is the one whose cost is dominated by the per-decision machinery.

\section{Reduction of the Ground Heuristic Component}

The central claim of the thesis concerns \emph{where} memory is spent, and the direct evidence is the number of ground heuristic objects as a function of $n$. The mechanism is readable in the encoding itself. The standard directive

\begin{minted}{prolog}
#heuristic b(X) : x(X), not c(X), S = #sum{Y : c(Y)},
    W = ((n+1)*S)+X. [W, true]
\end{minted}

carries the aggregate value $S$ \emph{as a variable inside the weight}. Since the atoms \mintinline{text}|c(Y)| are choice atoms, the grounder cannot know the sum at grounding time and must materialize one directive for every reachable pair $(X,S)$. With $X \in \{1,\ldots,n\}$ and $S$ ranging over the subset sums of $\{1,\ldots,n\}$, which is every integer from $0$ to $n(n+1)/2$ because $1$ belongs to the domain, the count is
\[
2 \cdot n \cdot \Big(\tfrac{n(n+1)}{2} + 1\Big) \;=\; \Theta(n^3),
\]
where the factor $2$ accounts for the two symmetric rules for \mintinline{text}|b| and \mintinline{text}|c|. The formula is verifiable exactly on the measured data: for $n=10$ it yields $2 \cdot 10 \cdot 56 = 1{,}120$, which coincides with the measured \mintinline{text}|ground_heuristics| value for \mintinline{text}|gc|. The explicit unrolling of \mintinline{text}|ga| materializes the same $(X,S)$ product by construction. The lazy variants remove this enumeration at the root: the aggregate is evaluated at solving time on the current partial assignment, and the materialized heuristics remain constant, two facts, independent of $n$.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{combined_heuristics}
    \caption{Comparison between native ground heuristic objects and lazy heuristic facts.}
    \label{fig:combined-heuristics}
\end{figure}

\begin{table}[H]
\centering
\small
\begin{tabular}{rrrr}
\toprule
\textbf{$n$} & \textbf{Native heuristic objects} & \textbf{Lazy facts} & \textbf{Ratio}\\
\midrule
10  & $1{,}120$       & 2 & $560\times$\\
50  & $127{,}600$     & 2 & $63{,}800\times$\\
100 & $1{,}010{,}200$ & 2 & $505{,}100\times$\\
110 & $1{,}343{,}320$ & 2 & $671{,}660\times$\\
\bottomrule
\end{tabular}
\caption{Growth of the native heuristic component compared with the lazy representation. The counts are properties of encoding and instance, independent of the machine.}
\label{tab:heur-growth}
\end{table}

This comparison must be read for what it is. For the ground variants the count is faithful: one line is one materialized object. For the lazy variants the two counted facts are \emph{templates} that expand at runtime into one candidate per ground target ($n$ candidates per rule for BSP); those candidates never pass through the grounder and therefore appear in no line count. The single-figure comparison \enquote{$2$ facts versus $\Theta(n^3)$ directives} is thus the proof of a saving in \emph{grounding space}, not of a saving in total work: the work moved to runtime is real and is measured separately in Section~\ref{sec:per-decision-cost}.

\section{BSP: Runtime Behaviour}
\label{sec:bsp-runtime}

Figure~\ref{fig:bsp-total} reports the total time as measured by clingo, and Figure~\ref{fig:bsp-solving} its solving component. The base program dominates the picture for every same-encoding variant: at $n=120$ the domain encoding grounds into $52.8$ million rules, a component that \mintinline{text}|gc_noheur|, \mintinline{text}|la| and \mintinline{text}|lc| pay alike, since they share the encoding verbatim except for the heuristic representation. Their grounding times at that size are $160.9$, $154.2$ and $159.7$ seconds, agreeing to within the few percent that Section~\ref{sec:time-reliability} measures as the reproducibility of the instrument. What the totals add on top of that shared component is the solving time, and it is there that the variants separate: $44.1$ seconds for \mintinline{text}|gc_noheur|, $47.5$ for \mintinline{text}|lc|, and $0.04$ for \mintinline{text}|la|. The \mintinline{text}|la| curve therefore runs below the other two over the whole range rather than crossing them, and the gap is the solving component that the next figure isolates. For the ground heuristics a second term is added on top, the grounding of the directives themselves.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{hpc/\ThesisGraphQuality/bsp/native_clingo_total.png}
    \caption{BSP, native backend: total clingo time. One run per point; a curve ends where its variant stops solving within the limits. The three same-encoding curves (\texttt{gc\_noheur}, \texttt{la}, \texttt{lc}) share a grounding component that agrees to a few percent at every size, so the vertical distance between them is their solving time: \texttt{la} runs below the other two throughout, and \texttt{gc\_noheur} and \texttt{lc} lie on top of each other. Figure~\ref{fig:bsp-solving} isolates that component.}
    \label{fig:bsp-total}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{hpc/\ThesisGraphQuality/bsp/native_solving.png}
    \caption{BSP, native backend: solving time only. \texttt{la} is indistinguishable from zero at every size; \texttt{ga} leaves the chart after $n=60$.}
    \label{fig:bsp-solving}
\end{figure}

\begin{table}[H]
\centering
\small
\begin{tabular}{lrrrrr}
\toprule
\textbf{Variant} & \textbf{Total (s)} & \textbf{Grounding est.\ (s)} & \textbf{Solving (s)} & \textbf{Choices} & \textbf{Conflicts}\\
\midrule
\mintinline{text}|gc_noheur| & 205.1 & 160.9 & 44.1 & $78{,}565$ & $49{,}993$\\
\mintinline{text}|gc|        & 458.2 & 167.7 & 290.5 & $231{,}260$ & $160{,}115$\\
\mintinline{text}|ga|        & \emph{T} & (172.6) & ($>$427.3) & $694{,}660$ & $510{,}480$\\
\mintinline{text}|ga_weak|   & 242.1 & 172.6 & 69.6 & $121{,}159$ & $80{,}847$\\
\mintinline{text}|la|        & 154.2 & 154.2 & 0.04 & 120 & 0\\
\mintinline{text}|lc|        & 207.2 & 159.7 & 47.5 & $78{,}565$ & $49{,}993$\\
\mintinline{text}|la_co|     & 0.009 & $<0.01$ & 0.01 & 117 & 0\\
\bottomrule
\end{tabular}
\caption{BSP at $n=120$, native backend, under the 600\,s / 30\,GB limits. \emph{T} marks a timeout; the parenthesized solving value for \texttt{ga} is a lower bound (total minus grounding at the moment it was killed), and its choices/conflicts are the counts reached before the kill. \texttt{gc\_noheur}, \texttt{la} and \texttt{lc} ground programs of the same size (about $52.76$ million rules), and \texttt{gc}, \texttt{ga} and \texttt{ga\_weak} programs of another common size (about $54.5$ million). Within each group the grounding column agrees to a few percent, and the solving column is what separates the variants: an order of magnitude between \texttt{gc} and the baseline, three orders between the baseline and \texttt{la}. The \texttt{la\_co} run is at the resolution limit of clingo's own timer. Choices and conflicts are exact. \texttt{la\_aux} is discussed in Section~\ref{sec:aux-form}.}
\label{tab:bsp-n120}
\end{table}

Three observations follow from Table~\ref{tab:bsp-n120}. First, \mintinline{text}|la| turns search into a formality, 120 choices and zero conflicts, so its total time \emph{is} the grounding time of the base program to within four hundredths of a second ($154.229$ against $154.189$). The claim this supports is stronger than a comparison of totals: the solving column, the only one the heuristic controls, drops from $44.1$ seconds on the baseline to $0.04$, a factor of a thousand, while every other cost in the run stays where the encoding put it. Under the raised time budget \mintinline{text}|gc| now finishes at $n=120$ as well, but pays $290.5$ seconds of solving where \mintinline{text}|la| pays $0.04$, and it is \mintinline{text}|gc| rather than \mintinline{text}|la| that first crosses into timeout territory as $n$ grows (Table~\ref{tab:overview-bsp}: \texttt{gc} fails from $n=140$, \texttt{la} from $n=170$, by which point the base program alone consumes the entire budget).

Second, \mintinline{text}|ga| is the surprise of the full campaign, and a negative one. Its static unrolling was designed as the faithful ground simulation of the Alpha reading, and up to $n=50$ it delivers exactly that: $n$ choices and zero conflicts at every size, indistinguishable from \mintinline{text}|la|. It then collapses. At $n=60$ it spends $136.8$ seconds of solving against $0.80$ of the baseline, with $1{,}668{,}059$ choices against $11{,}810$, and from $n=70$ it no longer finishes within the 600-second limit (at $n=120$ it was killed at $694{,}660$ choices and counting). The gap is more than two orders of magnitude on an exact structural measure.

The cause is not the grounding. Table~\ref{tab:heur-growth} shows that \mintinline{text}|ga| materialises the same $\Theta(n^3)$ directives as \mintinline{text}|gc|, and at $n=70$, the first size it fails, it grounds in $14.9$ seconds and $882$ MB against the $14.7$ seconds and $845$ MB of \mintinline{text}|la|: two tenths of a second and thirty-seven megabytes, on a budget of 600 seconds and 30~GB. The cause is the weight bound of Section~\ref{sec:flattening}. The unrolled directives carry the current value of the aggregate \emph{in the weight}, and from $n=51$ that value no longer fits the field clasp reserves for it, so the upper part of the sum range saturates at a single level and the reconstruction of the dynamic value stops working. Section~\ref{sec:ga-weak} isolates the mechanism and shows that the same encoding, rescaled to stay inside the bound, solves $n=70$ in $665$ choices. The datum therefore supports a sharper conclusion than a cost argument: inside a ground-and-solve pipeline a dynamic aggregate is not merely expensive to simulate, it ceases to be representable once the instance grows, which is why \mintinline{text}|ga| is the only variant of the four-variant matrix, ground or lazy, that fails before the heuristic-free baseline does, and by nine sizes (the exploratory \mintinline{text}|la_aux| fails there too, for the unrelated reason analysed in Section~\ref{sec:aux-form}).

Third, Table~\ref{tab:bsp-n120} contains two rows that the figures of this section deliberately omit. \mintinline{text}|ga_weak|, the cheap approximation of the Alpha reading, stays close to the baseline and is compared against the faithful \mintinline{text}|ga| in Section~\ref{sec:ga-weak}; \mintinline{text}|la_co|, which changes the problem model itself, is the subject of Section~\ref{sec:constr-form}.

\section{BSP: the Two Semantics Under the Microscope}
\label{sec:bsp-semantics}

Search statistics separate the two semantics sharply, and the full campaign adds an instrumentation-level proof of \emph{why}. The \mintinline{text}|la| variant makes exactly $n$ choices with zero conflicts at every size: the Alpha reading makes the greedy balance heuristic total, one direct decision per element, which is the known optimal strategy for this problem. The \mintinline{text}|lc| variant is not merely \enquote{close to the baseline}: it is \emph{search-identical} to it. At every size, its choice and conflict counts coincide with \mintinline{text}|gc_noheur| to the last unit ($78{,}565$ and $49{,}993$ at $n=120$).

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/native_choices.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/native_conflicts.png}
    \caption{BSP, native backend: choices and conflicts. \texttt{la} lies on the horizontal axis; \texttt{lc} coincides with the baseline exactly, at every size both solve, so the two curves are superimposed; \texttt{ga} leaves the chart after $n=60$. Unlike the time plots, these are exact structural counts.}
    \label{fig:bsp-search}
\end{figure}

The instrumentation explains the coincidence, and both backends now report it independently. On BSP the \mintinline{text}|lc| rule guards the aggregate source with the clingo reading: the sum over \mintinline{text}|c/1| becomes usable only when every \mintinline{text}|c| atom is determined, and \mintinline{text}|clingo_not(c(X))| additionally requires established falsity. During search these conditions are essentially never satisfied at decision time: at $n=120$ both backends record exactly $78{,}565$ decide calls --- one per solver choice, the propagator is consulted every single time --- with an average of \emph{zero} applicable candidates per call (\mintinline{text}|avg_candidates_per_decide| $= 0.0$, and \mintinline{text}|max_candidates_seen| $=0$, so not a single call in the whole run produced a candidate). Every decide call returns nothing, the solver falls back to its default heuristic, and the trajectory reproduces the baseline exactly. The clingo-like lazy variant on BSP is therefore \emph{inert by semantics}: it costs the full decide-loop machinery and buys nothing, which is precisely what the design predicts for a heuristic whose guards wait for information that only exists at the bottom of the search tree.

\section{BSP: Rules, Variables, and Memory}

The compression of the heuristic component is visible in the size of the propositional instance. At $n=100$, \mintinline{text}|gc| is fifty times larger in solver variables than every other variant, because its heuristic directives materialize the negative body and the aggregate bound for each $(X,S)$ pair. Notably, \mintinline{text}|ga| does \emph{not} inflate the variable count ($20{,}402$, in line with the baseline): its unrolled directives reuse the same aggregate structures, and its cost shows up as grounding time and unguarded activations instead. The two ground variants thus pay the same combinatorial product in two different currencies.

\begin{table}[H]
\centering
\small
\begin{tabular}{lr}
\toprule
\textbf{Variant} & \textbf{Variables at $n=100$}\\
\midrule
\mintinline{text}|gc| & $1{,}030{,}606$\\
\mintinline{text}|ga| & $20{,}402$\\
\mintinline{text}|ga_weak| & $20{,}406$\\
\mintinline{text}|lc| & $20{,}300$\\
\mintinline{text}|la| & $20{,}300$\\
\mintinline{text}|gc_noheur| & $20{,}300$\\
\bottomrule
\end{tabular}
\caption{Number of solver variables at $n=100$, native backend.}
\label{tab:n100-vars}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{hpc/\ThesisGraphQuality/bsp/native_mem.png}
    \caption{BSP, native backend: peak resident set size.}
    \label{fig:bsp-memory}
\end{figure}

The memory metric is the peak resident set size of the entire solver process, end to end: it covers grounding, CDCL search, and the custom propagator in a single value, and for the lazy Prolog variants it includes the in-process SWI-Prolog engine and its asserted database. It does not depend on the machine at all, and the three same-encoding variants agree to within $0.1\%$ at every size from $n=90$ upwards, which is itself a useful control on the metric. At large $n$ memory is dominated by the base program for every same-encoding variant: at $n=120$, \mintinline{text}|gc_noheur|, \mintinline{text}|la|, and \mintinline{text}|lc| sit at $6.58$, $6.58$ and $6.59$~GB, while \mintinline{text}|gc| reaches $7.4$~GB and \mintinline{text}|la_aux| $8.5$~GB. Under the 30~GB budget none of these is close to the ceiling at this size; the base program only approaches it much further out, at $n=180$, which is where Table~\ref{tab:overview-bsp} records the campaign's only memouts. The heuristic saving is real but bounded by the base program's own footprint; the variant that changes the footprint by orders of magnitude is \mintinline{text}|la_co| (Section~\ref{sec:constr-form}), because it changes the base program itself. On the Prolog side the SWI engine adds a fixed overhead of the order of ten megabytes, invisible at this scale and dominant only on the trivial \mintinline{text}|la_co| runs. The RSS \emph{confirms} the saving on the real consumption; the ground-object count of Table~\ref{tab:heur-growth} \emph{explains its cause}.

\section{The Price of Laziness: Per-Decision Cost}
\label{sec:per-decision-cost}

Intellectual honesty requires stating that the lazy approach does not \emph{eliminate} the cost of the combinatorial explosion: it \emph{moves} it. What ground-and-solve pays once, in memory, at grounding time, the lazy backend pays repeatedly, in time, at every decision. The full campaign turns this from a declared principle into measured numbers, and both backends are now phase-timed, so the cost can be attributed rather than inferred: the propagator reports, per run, its decide calls, the candidates each call produced, the cumulative time spent answering and the cumulative time spent keeping its state synchronized with the trail.

\begin{table}[H]
\centering
\footnotesize
\begin{tabular}{llrrrrrr}
\toprule
 & & & & \multicolumn{2}{c}{\textbf{ms/decide}} & & \\
\cmidrule(lr){5-6}
\textbf{Family} & \textbf{Variant} & \textbf{decide calls} & \textbf{cand./decide} & \textbf{native} & \textbf{Prolog} & \textbf{sync (s)} & \textbf{prop.\ share}\\
\midrule
BSP $n{=}120$ & \mintinline{text}|la| & 120 & 121 & $1.0\!\cdot\!10^{-4}$ & 0.62 & 3.4\,ms & $0.05\%$\\
BSP $n{=}120$ & \mintinline{text}|lc| & $78{,}565$ & 0.0 & $1.5\!\cdot\!10^{-4}$ & 0.21 & 3.30 & $8.7\%$\\
PUP $N{=}160$ & \mintinline{text}|la| & 197 & 5.3 & $3.8\!\cdot\!10^{-4}$ & 3.84 & 8.20 & $7.1\%$\\
PUP $N{=}200$ & \mintinline{text}|la| & 249 & 5.2 & $5.6\!\cdot\!10^{-4}$ & 6.74 & 16.53 & $7.3\%$\\
HRP $N{=}20$  & \mintinline{text}|la| & 106 & $1{,}248$ & $1.3\!\cdot\!10^{-4}$ & 9.68 & 0.035 & $68.3\%$\\
HRP $N{=}14$  & \mintinline{text}|lc| & $73{,}143$ & 239 & $0.9\!\cdot\!10^{-4}$ & 3.44 & 11.09 & $99.4\%$\\
\bottomrule
\end{tabular}
\caption{Propagator instrumentation on representative solved runs. \emph{decide calls} and \emph{cand./decide} are taken from the Prolog backend, which counts every candidate the \texttt{heuristic/4} query returns; \emph{ms/decide} is the average time of one consultation, reported by both backends; \emph{sync} is the cumulative cost of mirroring the solver trail into the Prolog database; \emph{prop.\ share} is the fraction of the Prolog run spent inside the propagator (whole decide callback plus synchronization). The native backend's own synchronization is not shown because on these runs it never exceeds $3.6$ seconds, reached on the inert \texttt{lc} cell, and on the well-guided \texttt{la} cells it stays in the milliseconds.}
\label{tab:per-decision}
\end{table}

Table~\ref{tab:per-decision} decomposes the overhead along its two multiplicative factors: how often the heuristic is consulted (decide calls, driven by the search trajectory) and how much each consultation costs (driven by the number of candidates the rules generate and by the size of the synchronized state). Before reading the corners, one figure dominates the table and deserves to be stated on its own: the per-consultation cost differs between the two backends by three to four orders of magnitude, from around $10^{-4}$ milliseconds for the compiled C++ evaluation against $0.2$ to $9.7$ milliseconds for the SWI-Prolog one. This is the price of generality, measured, and it is the reason the two backends occupy different points of the design space rather than competing on the same one.

The four corners of the space are all realized in the data. \mintinline{text}|la| on BSP is the benign corner: 120 consultations of six tenths of a millisecond each, $64$ milliseconds of query time on a run of nearly three minutes, $0.04\%$ of it. When the heuristic guides perfectly, an entire embedded logic-programming engine costs nothing measurable. \mintinline{text}|lc| on BSP multiplies a \emph{small} per-call cost by $78{,}565$ inert consultations; the query cost stays low (a fifth of a millisecond each), but the count is large enough that the propagator ends up owning $8.7\%$ of the run, $14.4$ seconds of queries and $3.3$ of synchronization, for a heuristic that never contributes a single decision. \mintinline{text}|la| on HRP multiplies \emph{few} consultations by a large per-call cost, since the HRP reference rules \cite{comploi2023domain} enumerate more than a thousand candidates per decision; on sub-second instances the $1.03$ seconds the propagator accumulates \emph{are} the run ($68.3\%$ of it), turning a $0.38$-second native run into a $1.55$-second Prolog one ($\times 4.1$). \mintinline{text}|lc| on HRP combines both factors, $73{,}143$ consultations at $3.4$ milliseconds each: $238.9$ of its $264.5$ seconds go into the queries and $11.1$ more into synchronization, $99.4\%$ of the run, against $1.80$ seconds for the native backend on the same instance, the largest cross-backend gap in the campaign ($\times 147$). Synchronization scales with the trail activity and the number of watched atoms, and is the one component that grows with instance size rather than with search: on PUP \mintinline{text}|la| it dominates the query cost by an order of magnitude ($8.20$ against $0.75$ seconds at $N{=}160$, $16.53$ against $1.67$ at $N{=}200$), which turns the batched-update protocol of the future work into a quantified target rather than a guess.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/prolog_decide_calls.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/prolog_ms_per_decide.png}
    \caption{BSP, Prolog backend: decide calls and per-decision query cost. The \texttt{lc} curve pairs tens of thousands of calls with a low unit cost; \texttt{la} the opposite.}
    \label{fig:bsp-instrumentation}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{hpc/\ThesisGraphQuality/bsp/comparison_solving.png}
    \caption{BSP: solving time, native (solid) versus Prolog (dashed) backend, lazy variants and ground reference.}
    \label{fig:bsp-comparison}
\end{figure}

The general law suggested by the table, and consistent with all 520 runs, is that the lazy overhead is the product $\textit{decide calls} \times (\textit{query cost} + \textit{amortized sync})$, where the first factor is governed by how well the heuristic guides (a good heuristic consults itself $n$ times; a bad or inert one, once per baseline choice) and the second by how much of the program the rules can see. The practical reading is that \emph{lazy heuristics are cheapest exactly when they work}: effective guidance keeps the decision count, and therefore the total overhead, small.

\section{Two Ground Simulations of Alpha: \texttt{ga} versus \texttt{ga\_weak}}
\label{sec:ga-weak}

The three subsections that follow analyze the exploratory settings. They are not alternative candidates in the main comparison: each one isolates a single design decision, and its figures include only the variants that decision touches.

The first decision is how faithfully the Alpha reading can be forced into a ground-and-solve pipeline. The two transformations of Section~\ref{sec:ga-simulation} are separable, and \mintinline{text}|ga_weak| applies only the first: it drops the negative literals, so that established truth no longer deactivates the directives, but keeps the equality binding $S = \#\mathtt{sum}\{Y : c(Y)\}$. \mintinline{text}|ga| applies both, replacing that equality with the monotone bound $S \le \#\mathtt{sum}\{Y : c(Y)\}$ unrolled over every candidate value, so that clasp's max-weight selection reconstructs the current value of the growing sum at solving time.

The distinction is \emph{not} one of grounding size, and this is worth stating because it is the natural guess. All three ground variants materialise the same number of heuristic directives: at $n=10$, $n=20$ and $n=30$ the aspif output of \mintinline{text}|gc|, \mintinline{text}|ga| and \mintinline{text}|ga_weak| contains exactly $1{,}120$, $8{,}440$ and $27{,}960$ heuristic statements respectively, matching the $\Theta(n^{3})$ formula in every case. Keeping the equality binding does not spare the grounder anything, because the sum ranges over the same subset sums either way. What the two forms differ in is \emph{when} a directive becomes applicable during search: under the equality binding at most one value of $S$ ever matches, and only once the sum is determined, whereas under the monotone bound every directive whose bound the partial sum has already passed is applicable at the same time.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/expl_gaweak_solving.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/expl_gaweak_choices.png}
    \caption{BSP, native backend, exploratory comparison: solving time (left) and solver choices (right) with both Alpha simulations alongside the main variants. \texttt{ga} is exact up to $n=50$, degrades from $n=51$ once its weights leave the representable range, needs $1.7$ million choices and $137$ seconds of solving at $n=60$, and times out from $n=70$; \texttt{ga\_weak} stays in the baseline's band over the whole range and reaches $n=140$ on both backends, two sizes short of the baseline.}
    \label{fig:gaweak}
\end{figure}

Figure~\ref{fig:gaweak} shows what each form costs at solving time. \mintinline{text}|ga_weak|, which keeps the equality binding, behaves like a heuristic that almost never fires: its solver variables stay at baseline level ($20{,}406$ against $20{,}300$ at $n=100$, since the equality binding needs no auxiliary structure per bound), and its search stays in the baseline's band: at $n=120$ it explores a noisier trajectory, $121{,}159$ choices against $78{,}565$, and pays for it with $69.6$ seconds of solving against the baseline's $44.1$. Dropping the negative guards on its own therefore neither guides nor helps: it costs the full $\Theta(n^{3})$ grounding of \mintinline{text}|gc|, adds half again as much solving as the variant with no heuristic at all, and gives up two sizes of reach ($n=140$ against $n=150$).

\mintinline{text}|ga|, which also unrolls the bound, is the variant that actually reproduces the dynamic aggregate, and up to $n=50$ it does so perfectly: $n$ choices and zero conflicts at every size, exactly like \mintinline{text}|la|. From $n=51$ it degrades, and by $n=60$ it needs $1{,}668{,}059$ choices and $136.8$ seconds of solving against $11{,}810$ choices and $0.80$ seconds of the baseline; from $n=70$ it never finishes within the 600-second limit. The break is not caused by the grounding, which at $n=70$ costs $14.9$ seconds and $882$ MB against $14.7$ seconds and $845$ MB for \mintinline{text}|la|, a difference of two tenths of a second and thirty-seven megabytes on a budget of 600 seconds and 30~GB. It is caused by the weight bound of Section~\ref{sec:flattening}. The BSP weight $(n+1)\cdot S + X$ has to represent every value the greedy sum can reach, roughly $n(n+1)/4$ per side, so the ranking survives only while $n(n+1)^{2} \le 4 \cdot 32{,}767$, that is up to $n=50$. Beyond that the upper part of the aggregate range saturates at a single level and the reconstruction of the dynamic value stops working, first partially and then almost entirely. Rescaling the same encoding so that its weights stay inside the bound confirms the diagnosis: \mintinline{text}|ga| then solves $n=70$ in $665$ choices instead of timing out.

Read together, the two simulations delimit the ground approach from both sides. The half of the Alpha reading that is cheap to obtain, the activation rule of \mintinline{text}|ga_weak|, is worse than useless: it perturbs the search without directing it, costing solving time and two sizes of reach against the variant that carries no heuristic at all. The half that reproduces the guidance, the unrolled aggregate of \mintinline{text}|ga|, works only while the reconstructed value is representable in clingo's weight field, and neither form spares the grounder anything. The lazy propagator obtains the same guidance without the unrolling and without the bound, which is the design point it occupies: at $n=120$, $0.04$ seconds and $120$ choices for the aggregate evaluated lazily, $69.6$ seconds and $121{,}159$ choices for the same aggregate bound by equality at grounding time, and a timeout for the same aggregate unrolled.

\section{Direct and Auxiliary Forms}
\label{sec:aux-form}

The \mintinline{text}|la_aux| variant routes the lazy heuristic through auxiliary predicates that materialize the heuristic body, including the aggregate value, before the propagator is used. This experiment separates two effects that are otherwise easy to confuse: reducing the number of ground heuristic directives, and avoiding the materialization of the heuristic body itself. \mintinline{text}|la_aux| keeps only two \mintinline{text}|__heuristic| facts, but its auxiliaries \mintinline{text}|h_b(X,S)| and \mintinline{text}|h_c(X,S)| reintroduce the $(X,S)$ product in the base program, and the full campaign shows the consequences from three angles at once. In the grounder, the auxiliary atoms inflate the instance ($145{,}600$ solver atoms and $132{,}756$ variables at $n=50$, against $10{,}352$ and $5{,}150$ for \mintinline{text}|la|). In the propagator, every $(X,S)$ atom becomes a runtime candidate, so the per-decision scan grows with $n^2$: the native backend already spends $287.1$ seconds of \emph{solving} to finish $n=60$ (against $0.00$ for \mintinline{text}|la|), and from $n=70$ it times out at the raised 600-second limit; its peak RSS keeps climbing with $n$ regardless ($8.5$~GB already at $n=120$ and $27.2$~GB at $n=170$, where the auxiliary relation alone puts five million variables into the propositional instance). On the Prolog side the same candidate flood must additionally cross the synchronization bridge, and there it becomes the whole run: at $n=20$ it already needs $2.9$ seconds of cumulative synchronization for $573$ decide calls, against $0.3$ milliseconds for the $20$ calls of \mintinline{text}|la|, and at $n=30$ synchronization alone accounts for $383$ of the $386$ seconds of the run ($99.2\%$), with the queries themselves costing $24$ milliseconds in total. The variant times out from $n=40$. The signature is worth naming, because it is the opposite of the HRP one: there the propagator was expensive because each query was expensive; here each query is \emph{cheap} ($0.02$ ms, the cheapest in the campaign) and the cost is entirely in keeping a state that the auxiliary relation made large. The lesson is clear: if the heuristic body is first materialized into a large auxiliary relation, the intended benefit of laziness is lost on every axis at once, and it is the search cost of the reintroduced product, not memory alone, that dominates the failure. The direct lazy form is the meaningful one.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/expl_laaux_solving.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/expl_laaux_decide_calls.png}
    \caption{BSP, exploratory comparison of the auxiliary form. Left: native solving time; \texttt{la\_aux} diverges from $n=40$ and leaves the chart after $n=60$, while the direct \texttt{la} stays on the axis. Right: propagator decide calls on the Prolog backend; the auxiliary candidates multiply the consultations ($573$ at $n=20$ and $8{,}452$ at $n=30$, against the $n$ calls of the direct form), and the curve ends at $n=30$, the last size the variant solves on that backend.}
    \label{fig:laaux}
\end{figure}

\section{Effect of the Constraint Formulation}
\label{sec:constr-form}

The \mintinline{text}|la_co| variant changes not only the heuristic representation but also the BSP constraint itself, replacing the quadratic difference check over \mintinline{text}|sumB|/\mintinline{text}|sumC| with a single interval constraint over one sum, whose bounds are compile-time constants. The effect is large enough to state exactly: at $n=120$ the propositional instance drops from $52{,}759{,}258$ rules to $366$ and from $29{,}160$ variables to $121$, and the clingo time from $205.1$ seconds (baseline) or $154.2$ seconds (\mintinline{text}|la|) to $9$ milliseconds, with the same 117 guided choices and zero conflicts, on both backends (the Prolog run measures $108$ milliseconds, the difference being the fixed engine startup). The same holds at every size up to $n=200$, where \mintinline{text}|la_co| still finishes in $14$ milliseconds with $197$ choices while every other variant has been timing out for four or five sizes. This measurement completes the diagnosis of Section~\ref{sec:bsp-runtime}: once the lazy heuristic has removed the search cost, the \emph{entire} residual cost of BSP is the ground materialization of the balance check, and a formulation that avoids it removes the last $\Theta$-large term. A comparison between \mintinline{text}|la_co| and \mintinline{text}|la| therefore measures the interaction between lazy heuristics and problem modelling, not the isolated effect of the heuristic representation, and it is reported as a modelling experiment: its role is to show what the combination \enquote{dynamic heuristic + propagation-friendly model} can reach when neither component pays a combinatorial ground price.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/expl_laco_grounding.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/bsp/expl_laco_rules.png}
    \caption{BSP, native backend, exploratory comparison of the constraint reformulation: grounding time (left) and propositional rules (right). \texttt{gc}, \texttt{la}, and \texttt{lc} overlap because they share the quadratic base encoding (some $52.8$ million rules and one to three minutes of grounding at $n=120$); \texttt{la\_co} lies on the horizontal axis of both plots ($366$ rules, milliseconds of grounding), the visual form of the five-orders-of-magnitude gap discussed in the text.}
    \label{fig:laco}
\end{figure}

\section{PUP: the Grounding Bottleneck at Scale}
\label{sec:pup}

PUP is the family where the grounding bottleneck, not the search, sets the frontier, and where the lazy representation translates directly into a large saving in time and memory at every common size. Figure~\ref{fig:pup-total} and Table~\ref{tab:pup-frontier} summarize the campaign under the 600-second, 30~GB limits; under this raised budget both Alpha-like variants solve the entire tested range up to $N=200$, so the frontier that used to separate them has become a cost gap instead.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{hpc/\ThesisGraphQuality/pup/native_clingo_total.png}
    \caption{PUP, native backend: total clingo time. Each curve ends at the last instance its variant solves.}
    \label{fig:pup-total}
\end{figure}

\begin{table}[H]
\centering
\footnotesize
\begin{tabular}{lrrrrr}
\toprule
\textbf{Variant} & \textbf{max $N$ solved} & \textbf{Total (s)} & \textbf{Grounding (s)} & \textbf{Peak RSS} & \textbf{first failure}\\
\midrule
\mintinline{text}|gc_noheur| & 80  & 123.2  & 10.6  & 0.7 GB & T@100\\
\mintinline{text}|gc|        & 60  & 97.0   & 17.7  & 1.3 GB & T@80\\
\mintinline{text}|ga|        & 200 & 445.1  & 436.6 & 24.6 GB & ---\\
\mintinline{text}|la|        & 200 & 231.4  & 225.8 & 12.7 GB & ---\\
\mintinline{text}|lc|        & 100 & 241.5  & 27.0  & 2.1 GB & T@80 (non-mon.)\\
\bottomrule
\end{tabular}
\caption{PUP frontier, native backend, 600\,s / 30\,GB per run: largest \texttt{double-$N$} solved and its cost. \texttt{ga} and \texttt{la} solve the whole tested range and are reported at $N=200$; the other three fail inside the range, and are reported at the largest size they solve. Unlike the BSP table, the separations here are between programs of different size: \texttt{ga} grounds twice the memory of \texttt{la} at every common size, a factor two orders of magnitude above the few-percent resolution established in Section~\ref{sec:time-reliability}.}
\label{tab:pup-frontier}
\end{table}

The ordering of the frontiers tells most of the story, and the part that changes with scale tells the rest. The baseline stalls at $N=80$ by \emph{time}: with no guidance, search explodes first ($546{,}549$ choices at $N=80$, against 272 for \mintinline{text}|la| at $N=200$, two and a half times that size). The plain ground heuristic \mintinline{text}|gc| fares worse still, failing already at $N=80$ and never recovering: its directives are cheap to ground but do not carry the dynamic aggregate, so they neither prevent the search explosion ($865{,}136$ choices before the kill at $N=80$, against the baseline's $546{,}549$ on the instance it does solve) nor pay for themselves. Both Alpha-like variants clear the whole tested range under the raised budget, at markedly different cost. \mintinline{text}|ga| pays for its static unrolling on every instance: at $N=120$ it needs $103.1$ seconds and $6.9$~GB against \mintinline{text}|la|'s $47.9$ seconds and $3.4$~GB, a $2.2\times$ time and $2.0\times$ memory gap that persists at every common size, because the unrolled directives grow with the value domains being unrolled rather than only with the instance; at $N=200$ it is $445.1$ against $231.4$ seconds ($1.92\times$) and $24.6$ against $12.7$~GB ($1.94\times$). Both variants show the same near-perfect search quality ($235$ choices and zero conflicts for \mintinline{text}|ga| against $162$ choices and $16$ conflicts for \mintinline{text}|la| at $N=120$; both semantics-Alpha), so the whole gap belongs to the heuristic's own grounding, as the mechanism predicts. Two independent facts make this the most robust comparison in the campaign: the gap is a stable factor of two across ten sizes, two orders of magnitude above the measurement resolution of Section~\ref{sec:time-reliability}, and it appears identically in peak RSS, which does not depend on the machine at all. Extrapolating the trend, \mintinline{text}|ga| is the variant one would expect to hit a memory wall first if the range were pushed further, since it reaches $24.6$ of the available $29.3$~GiB already at $N=200$ while \mintinline{text}|la| is at $12.7$; the campaign as run does not observe that wall directly, but the ratio is the same signature that, under the previous 8~GiB budget, made \mintinline{text}|ga| the first of the two to fail.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/pup/native_grounding.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/pup/native_mem.png}
    \caption{PUP, native backend: grounding time and peak memory. \texttt{ga} pays the heuristic unrolling (orange) on top of the base growth; \texttt{la} (green) pays the base program only, and stays consistently cheaper at every common size.}
    \label{fig:pup-ground-mem}
\end{figure}

The clingo-semantics cells complete the picture from below. Native \mintinline{text}|lc| oscillates around the baseline, solving $N=100$ (which the baseline misses) but failing at $N=80$ (which the baseline solves): as on BSP, its gated aggregates never produce candidates (on every instance it solves, both backends report \emph{zero} candidates and a \mintinline{text}|max_candidates_seen| of zero, over $233$ and $36{,}325$ native decide calls). Unlike BSP, however, the trajectory does diverge from the baseline --- $36{,}325$ choices against $11{,}659$ at $N=40$, and $174{,}593$ at $N=100$ on an instance where the baseline instead times out after $3{,}312{,}601$ --- which is at first sight a contradiction: a propagator that never returns a candidate cannot change a decision. The resolution is in the ground programs. On BSP the lazy encoding and the baseline instantiate the same program up to the two \mintinline{text}|__heuristic| facts ($52{,}759{,}258$ rules against $52{,}759{,}256$), which is why an inert propagator there implies a bit-identical trajectory. On PUP the lazy encoding needs auxiliary predicates to expose the aggregate sources, so it grounds a visibly different instance ($216{,}193$ rules and $51{,}776$ variables at $N=20$, against $161{,}018$ and $5{,}298$ for the baseline): clasp sees a different formula and, on a family this sensitive to it, takes a different trajectory from the first restart. The perturbation is therefore incidental rather than heuristic in origin, it moves in both directions, and it is the honest reading of the non-monotone frontier reported in Table~\ref{tab:overview-pup}.

What this same fact buys, however, is the cleanest semantics experiment of the campaign. On PUP \mintinline{text}|la| and \mintinline{text}|lc| ground \emph{exactly} the same program --- identical rules, atoms, variables and constraints at all ten sizes --- and differ only in the \mintinline{text}|__semantics| token that selects how the propagator reads partial information. Everything that separates them is therefore the semantics and nothing else: at $N=120$, $162$ choices and $47.9$ seconds against $53{,}036$ choices and a timeout; at $N=200$, $272$ choices against a timeout. The same comparison on the ground side is impossible to set up, because \mintinline{text}|ga| and \mintinline{text}|gc| necessarily differ in their unrolling as well. On the Prolog backend the same inert consultations acquire a price ($25{,}197$ calls and $72.3$ seconds of synchronization, $72\%$ of the run, already at $N=40$), and \mintinline{text}|lc| collapses to $N=40$: the pathological corner of Section~\ref{sec:per-decision-cost} reproduced at scale.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/pup/comparison_solving.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/pup/prolog_state_sync.png}
    \caption{PUP: native-versus-Prolog solving comparison (left) and cumulative synchronization cost of the Prolog backend (right). For \texttt{la} the synchronization, not the query, is the dominant lazy cost at scale.}
    \label{fig:pup-comparison}
\end{figure}

For the well-behaved \mintinline{text}|la|, the cross-backend comparison isolates the pure cost of the general-purpose engine: the guidance is equivalent (both backends solve the whole range, with search statistics within a few percent of each other --- $217$ against $197$ choices at $N=160$ --- the residual difference being the tie-break discussed at the start of this chapter), and the Prolog total grows from $112.7$ to $126.6$ seconds at $N=160$ ($+12\%$) and from $231.4$ to $248.6$ at $N=200$ ($+7\%$), of which $8.2$ and $16.5$ seconds respectively are trail synchronization, against $0.75$ and $1.67$ of query time. At PUP scale the bottleneck of the embedded engine is \emph{keeping the state} rather than answering the queries: it costs ten times the queries it serves, which points at the batched-synchronization optimization discussed in future work. The native backend, whose synchronization is incremental and in-process, spends $0.68$ seconds on the same task at $N=200$.

\section{HRP: Semantics Without Aggregates}
\label{sec:hrp}

HRP is the negation-heavy benchmark: its five-level reference heuristic \cite{comploi2023domain} contains no aggregates, so the \mintinline{text}|la|/\mintinline{text}|lc| comparison isolates the negation semantics alone. At the benchmark sizes the problem is easy for clasp, every variant except \mintinline{text}|lc| solves every instance in well under a second on the native backend, which makes HRP useless for frontier claims but ideal as a controlled experiment on guidance quality and per-decision cost.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/hrp/native_choices.png}
    \hfill
    \includegraphics[width=0.49\textwidth]{hpc/\ThesisGraphQuality/hrp/comparison_solving.png}
    \caption{HRP: solver choices on the native backend (left) and the native-versus-Prolog solving comparison (right). The only diverging curve is \texttt{lc}, in both plots.}
    \label{fig:hrp}
\end{figure}

The Alpha reading behaves exactly as designed: \mintinline{text}|la| tracks \mintinline{text}|ga| decision for decision (86 versus 88 choices at $N=14$, zero conflicts for both, and the same agreement at every size), confirming on a second problem class that the lazy propagator reproduces the intended Alpha guidance. The clingo reading, by contrast, is \emph{actively toxic} on this heuristic: requiring established falsity on guards such as \mintinline{text}|not fullCabinet(C)| delays every level of the policy until the corresponding closure information exists, and the surviving low-level modifications steer the solver into a pathological region. Native \mintinline{text}|lc| needs $66{,}717$ choices and $32{,}353$ conflicts at $N=14$, almost eight hundred times the $86$ choices of \mintinline{text}|la|, and the divergence is super-exponential in $N$: at $N=16$ it takes $14{,}218{,}384$ choices and $8{,}136{,}557$ conflicts, a factor of two hundred over the previous size, which it still just manages to close in $441.0$ of the 600 available seconds; from $N=18$ it times out. The Prolog twin is one size behind, timing out already from $N=16$.

Two details of this comparison are worth stating precisely, because they qualify how much the frontier can be made to carry. First, the two backends do \emph{not} reproduce identical trajectories here: at $N=14$ native \mintinline{text}|lc| records $66{,}717$ choices and $32{,}353$ conflicts against the Prolog twin's $73{,}143$ and $41{,}471$, and similar few-percent gaps appear at every size. HRP is the family in which the heuristic produces hundreds of equally ranked candidates per decision, so the deterministic tie-break of the two implementations resolves them differently, and once a search this unstable has taken one different decision the counts drift apart. What certifies the collapse as a property of the \emph{semantics} rather than of one engine is therefore not the identity of the counts but the fact that both backends reach the same pathological order of magnitude, four to five orders above \mintinline{text}|la|, from encodings that differ only in the semantics token. Second, and for the same reason, the one-size gap between the frontiers is not purely per-decision cost: part of it is the different trajectory. The claim the data supports is the qualitative one --- the clingo reading destroys this heuristic on both backends --- and it is a strong claim precisely because it survives the change of engine. Where BSP showed the clingo reading as inert, HRP shows it as misleading: the two failure modes of \enquote{late} information are both on record.

The Prolog backend adds the cost dimension, and HRP is where it is most visible. The reference rules enumerate every applicable (cabinet, thing) and (room, cabinet) pair, more than a thousand candidates per decision at $N=20$, so even the well-guided \mintinline{text}|la| pays $9.7$ milliseconds per consultation, the most expensive query in the campaign, and spends $68\%$ of a $1.55$-second run inside the propagator, against $0.38$ seconds for the native backend, whose compiled candidate scan answers the same query in $10^{-4}$ milliseconds. The misguided \mintinline{text}|lc| multiplies $3.4$ milliseconds by $73{,}143$ consultations: $238.9$ of its $264.5$ seconds at $N=14$ are spent inside candidate-rich Prolog queries and $11.1$ more in synchronization, $99.4\%$ of the run, against $1.80$ seconds for the native backend on the same instance. That $\times 147$ ratio is the largest in the whole campaign, and it measures the evaluation engine rather than the heuristic: the search it drives is equally bad on both sides, and only the price of asking differs.

\section{An External Reference: Alpha}
\label{sec:alpha}

Everything measured so far compares clingo against clingo. It establishes what the lazy representation costs and what the Alpha reading buys \emph{inside} a ground-and-solve system, but it cannot say how far that gets one from the system whose semantics is being reproduced. The runs of Section~\ref{sec:alpha-setup} answer that question directly, on the two families whose heuristics carry dynamic aggregates. As stated there, only wall-clock time and peak RSS are commensurable across the two systems, and Alpha's memory is inflated by the reserved JVM heap; the internal counters of the two solvers are not compared at any point below, and the frontiers are read as lower bounds within the campaign's limits.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{hpc/\ThesisGraphQuality/alpha/bsp_dashboard.png}
    \caption{BSP: the four clingo variants and the heuristic-free clingo baseline against Alpha with and without its declarative heuristic. Left, wall time; right, peak RSS. The \texttt{Alpha Qh} curve lies on the horizontal axis of both plots for the whole range; \texttt{Alpha (no heur)} instead leaves it, and stops at $n=40$, earlier than any clingo variant.}
    \label{fig:alpha-bsp}
\end{figure}

On BSP the picture is unambiguous, and it is not about the heuristic. Alpha with its heuristic solves all twenty sizes, from $0.7$ seconds and $0.11$~GB at $n=10$ to $3.4$ seconds and $0.37$~GB at $n=200$, with zero backtracks at every size and a number of guesses equal to $n$ --- the same shape of trajectory that \mintinline{text}|la| produces inside clingo ($n$ choices, zero conflicts), obtained by the system the lazy propagator imitates. Where the two diverge is everything around the search: at $n=160$, the largest size the clingo lazy variant reaches, \mintinline{text}|la| needs $585.7$ seconds and $19.8$~GB against Alpha's $2.4$ seconds and $0.35$~GB, and from $n=170$ every clingo variant except \mintinline{text}|la_co| is out of the budget while Alpha's cost is still growing linearly and is still under four seconds at $n=200$.

That gap is the term Section~\ref{sec:bsp-runtime} isolated and could not remove. \mintinline{text}|la| already spends $0.04$ seconds of solving on a $154$-second run at $n=120$: there is no search left to save, and the whole difference is the $52.8$ million rules that gringo materialises at that size and that Alpha never builds, because it instantiates only what the search touches. The result is therefore not a defeat of the mechanism but a measurement of its declared scope --- the propagator acts on the heuristic component and the base program is still grounded --- and it is the same conclusion that \mintinline{text}|la_co| reached from the other direction in Section~\ref{sec:constr-form}, by removing the $\Theta$-large term from the model rather than from the pipeline. Two independent routes, one to $n=200$ in milliseconds and one to $n=200$ in seconds, agree that on BSP the residual wall belongs to the domain encoding.

The comparison also settles a question the matrix leaves open, and it settles it in the thesis's favour. Lazy grounding on its own is \emph{not} what carries Alpha: \mintinline{text}|alpha_noheur| stops at $n=40$, after $42{,}058$ guesses and $42{,}021$ backtracks, eleven sizes before the ground clingo baseline stops --- at a size clingo grounds and solves in under two seconds. Reading the four combinations together --- eager grounding without a dynamic heuristic ($n=150$), eager grounding with one ($n=160$, with the search cost removed), lazy grounding without one ($n=40$), lazy grounding with one ($n=200$) --- shows that the two ingredients are separable and that neither is sufficient alone. The semantics contribution measured throughout this chapter is thus not an artefact of a weak baseline: it is the same contribution that makes the reference system work, isolated in a setting where grounding is held fixed.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{hpc/\ThesisGraphQuality/alpha/bsp_internals.png}
    \caption{BSP, Alpha's own counters, plotted apart from every clingo measure because they are not commensurable with clasp's (Section~\ref{sec:alpha-setup}). Top row: with the heuristic, guesses stay equal to $n$ and backtracks at zero over the whole range, while the heuristic-free run spikes to some $67{,}000$ of each at $n=30$ and stops at $n=40$. Bottom row: the cost of the heuristic itself, which is a Prolog query in this variant --- $2.2$ seconds cumulative at $n=200$, at $1.9$ to $5.4$ milliseconds per query.}
    \label{fig:alpha-internals}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{hpc/\ThesisGraphQuality/alpha/pup_dashboard.png}
    \caption{PUP: the same comparison. \texttt{Alpha (no heur)} does not appear because it solves no instance at all, not even \texttt{double-20}. Here the clingo lazy variant stays within a small factor of Alpha in time and matches it in memory, while the ground simulation \texttt{ga} is worse than Alpha on both axes.}
    \label{fig:alpha-pup}
\end{figure}

PUP tells the more interesting half of the story, because there the clingo side is competitive. Alpha solves the whole range with zero backtracks, from $2.1$ seconds at $N=20$ to $50.8$ seconds and $10.6$~GB at $N=200$. At that size \mintinline{text}|la| costs $234.7$ seconds and $12.7$~GB: a factor of $4.6$ in time and $1.2\times$ the memory. The memory comparison should be read in Alpha's favour rather than as a tie, since its figure includes the JVM heap reserved through \mintinline{text}|-Xmx| and therefore over-estimates what its algorithm holds; the lazy propagator comes close on that axis without matching it. The ground simulation of the same semantics, \mintinline{text}|ga|, costs $451.4$ seconds and $24.6$~GB, nine times Alpha's time and more than twice its memory. The ordering is the one the mechanism predicts: moving the heuristic out of the grounder recovers most of the distance to the native lazy-grounding system, and the part it does not recover is again the base program, since $225.8$ of \mintinline{text}|la|'s $231.4$ seconds of clingo time at $N=200$ are grounding.

Here too the heuristic-free control is decisive, and more sharply than on BSP: \mintinline{text}|alpha_noheur| solves \emph{nothing}, not even the smallest instance, so on this family the declarative heuristic is the entire reason the reference system is usable at all. The same heuristic, transplanted into clingo and evaluated lazily, gets within a factor of four and a half of it; transplanted and unrolled at grounding time, it ends up nine times slower and more than twice as hungry as the system it was copied from.

\begin{table}[H]
\centering
\small
\begin{tabular}{llrrr}
\toprule
\textbf{Family} & \textbf{System} & \textbf{max solved} & \textbf{Wall (s)} & \textbf{Peak RSS}\\
\midrule
BSP & \mintinline{text}|alpha_noheur|            & 40  & 30.5  & 1.1 GB\\
BSP & clingo \mintinline{text}|gc_noheur|        & 150 & 563.6 & 15.5 GB\\
BSP & clingo \mintinline{text}|la|               & 160 & 586.5 & 19.8 GB\\
BSP & \mintinline{text}|alpha| (Qh)              & 200 & 3.4   & 0.37 GB\\
BSP & clingo \mintinline{text}|la_co|            & 200 & 0.04  & $<0.1$ GB\\
\midrule
PUP & \mintinline{text}|alpha_noheur|            & --- & ---   & ---\\
PUP & clingo \mintinline{text}|gc_noheur|        & 80  & 123.3 & 0.7 GB\\
PUP & clingo \mintinline{text}|lc|               & 100 & 242.2 & 2.1 GB\\
PUP & clingo \mintinline{text}|ga|               & 200 & 451.4 & 24.6 GB\\
PUP & clingo \mintinline{text}|la|               & 200 & 234.7 & 12.7 GB\\
PUP & \mintinline{text}|alpha| (Qh)              & 200 & 50.8  & 10.6 GB\\
\bottomrule
\end{tabular}
\caption{Largest instance solved within the 600\,s / 30\,GB budget, and its cost, ordered by reach within each family. Wall time and peak RSS are \texttt{runlim} measures, the only ones commensurable across the two systems; Alpha's RSS includes the reserved JVM heap. clingo rows are the native backend. A dash marks a system that solved no instance of the family.}
\label{tab:alpha-frontier}
\end{table}

One last observation concerns the evaluation engines rather than the systems, and it is possible only because the two designs happen to coincide: Alpha's Qh mode and the Prolog backend of this thesis both answer the heuristic by querying a Prolog engine over an image of the partial assignment. The per-consultation costs are of the same order. At BSP $n=120$ Alpha issues $242$ queries at $2.87$ milliseconds each, against $120$ consultations at $0.62$ milliseconds for the Prolog backend; at PUP $N=200$, $1{,}129$ queries at $7.10$ milliseconds against $249$ at $6.74$. The two counts measure different events --- Alpha queries around its own guesses, the propagator once per clasp decision --- so only the order of magnitude is meaningful, but it is enough to say that the synchronisation-based design of Section~\ref{sec:prolog-syntax} does not pay a penalty against the reference implementation of the same idea, and that the three-to-four orders of magnitude separating it from the native backend (Section~\ref{sec:per-decision-cost}) are the price of Prolog evaluation as such, not of this particular way of arranging it.

\section{Native versus Prolog: Summary}
\label{sec:backend-summary}

Across the full campaign the two backends put the frontier in the same place in every cell except five, and in all five the Prolog backend is the one that stops earlier: \mintinline{text}|la| on BSP ($n=160\to150$), \mintinline{text}|lc| on BSP ($150\to140$), \mintinline{text}|la_aux| on BSP ($n=60\to30$), \mintinline{text}|lc| on PUP ($N=100\to40$) and \mintinline{text}|lc| on HRP ($N=16\to14$). The ordering has no exception, which is what the design predicts: the two backends ground the same program and differ only in what a consultation costs, so the Prolog side can lose reach and cannot gain any. Three of the five gaps are large and are the per-decision cost measured directly (Section~\ref{sec:per-decision-cost}); the two one-size gaps on \mintinline{text}|la| and \mintinline{text}|lc| are decided by native runs that finish at $585.7$ and $595.0$ seconds against the 600-second limit, inside the margin of Section~\ref{sec:time-reliability}, and no claim is built on them. Everywhere the propagator is cheap and the frontier is set by grounding, the two backends coincide: on PUP and HRP \mintinline{text}|la| solves the entire range on both.

For every ground variant the two systems run byte-identical encodings on the same solver, and their choice and conflict counts agree to the unit on all three families, which doubles as an end-to-end sanity check of the whole pipeline. For the lazy variants the native backend is faster per consultation by three to four orders of magnitude. Total times are the wrong instrument for that comparison, because they are dominated by a grounding component the two backends share: at $n=120$ the Prolog \mintinline{text}|gc_noheur| total is $199.5$ seconds against the native $205.1$, a $2.7\%$ difference on a pair of runs doing identical work, and at $n=130$ Prolog \mintinline{text}|la| finishes in $231.0$ seconds against $235.1$ on a run whose propagator cost $89$ milliseconds in all. Where the propagator's share of the run is large, the comparison becomes meaningful and always favours the native backend: on the twelve cells of the main matrix in which the propagator owns more than half of the run, the Prolog total is between $1.4$ and $147$ times the native one. The Prolog backend remains the more expressive and the more observable one, which is the trade-off it was designed to embody.

\section{Summary of Results}

The full campaign supports eight conclusions. First, the lazy representation reduces the ground heuristic component from $\Theta(n^3)$ directives to two constant facts on BSP, and the saving is confirmed on real memory wherever the heuristic grounding is a significant share of the footprint, culminating in PUP where, under a raised 600-second/30~GB budget that both Alpha-like variants clear over the entire tested range ($N=20$ to $200$), the lazy variant costs half the time and half the peak memory of the ground heuristic simulation at every one of the ten common sizes. Peak memory carries this claim on its own, since it depends on the program rather than on the machine.

Second, the total runtime does not become constant, because the base program is still fully grounded, and on BSP this is now the \emph{only} thing that sets the frontier: \mintinline{text}|gc_noheur|, \mintinline{text}|la| and \mintinline{text}|lc| stop within a band two sizes wide, between $n=140$ and $n=160$, and what distinguishes them there is not reach but cost, since \mintinline{text}|la| arrives at the common wall having spent $0.04$ seconds of solving against the baseline's $44.1$. The lazy mechanism isolates the heuristic from the growth of the instance, no more and no less; where the instance itself is the bottleneck, the correct claim is a thousandfold reduction of the search component, not a better frontier.

Third, the semantics axis is not a detail: the Alpha reading turns the BSP heuristic into a perfect greedy strategy ($n$ choices, zero conflicts, at every size) and reproduces the intended reference policy on HRP decision for decision, while the clingo reading is inert on BSP (bit-identical to the baseline, with a maximum of zero candidates over $78{,}565$ consultations) and actively harmful on HRP (four to five orders of magnitude more choices, on both backends independently). PUP settles the point in the cleanest possible form, because there \mintinline{text}|la| and \mintinline{text}|lc| ground literally the same program and differ by one token: $272$ choices against a timeout. Representation and semantics must never be conflated.

Fourth, the faithful \emph{ground} simulation of the Alpha reading (\mintinline{text}|ga|) reproduces the lazy guidance exactly while its weights remain representable, and stops working once they do not. On BSP it is exact up to $n=50$, degrades from $n=51$, and from $n=70$ is the only variant of the main matrix to fail before the heuristic-free baseline does, by nine sizes, because the reconstructed value of the aggregate no longer fits clingo's weight field. On PUP, where it does fit, it survives the whole tested range but costs twice the time and twice the memory of the lazy variant at every size. Taken together, this makes the lazy propagator not merely cheaper but the only practical carrier of that semantics inside clingo at scale.

Fifth, laziness is a space--time trade-off whose runtime side is now measured on both backends, not merely declared: the overhead factorizes as decide calls times per-consultation cost, the propagator's share of a run ranges from nothing (BSP \mintinline{text}|la|, $64$ milliseconds of query time on a run of nearly three minutes) to $99.4\%$ (HRP \mintinline{text}|lc|), the cost of one consultation differs between the compiled and the interpreted backend by three to four orders of magnitude, and the total is smallest exactly when the heuristic guides well.

Sixth, the three exploratory controls delimit the approach from every side: \mintinline{text}|ga_weak| shows that dropping the negative guards, without also unrolling the aggregate, adds search cost and loses two sizes of reach against the heuristic-free baseline while still paying the full $\Theta(n^{3})$ grounding, \mintinline{text}|la_aux| shows that materializing the heuristic body forfeits the benefit entirely and on the Prolog side turns synchronization into $99\%$ of the run, and \mintinline{text}|la_co| shows that pairing the lazy heuristic with a propagation-friendly model eliminates the residual ground cost and solves the entire range, $n=200$ included, in milliseconds.

Seventh, the two backends agree on every scientific conclusion and on every frontier except five cells, in all of which the Prolog backend is the one that stops earlier, so the choice between them is an engineering trade-off rather than a correctness question.

Eighth, the external comparison with Alpha places all of the above on an absolute scale and separates its two ingredients. The lazy propagator reproduces the reference system's guidance --- on BSP the same $n$-decision, backtrack-free trajectory --- and on PUP comes within a factor of $4.6$ in time and $1.2\times$ its peak memory, while the ground simulation of the same semantics is nine times slower and more than twice as hungry as the reference; on BSP the residual gap is two orders of magnitude and is entirely the grounding of the base program, which is the mechanism's declared scope limit and the same term that \mintinline{text}|la_co| removes from the other side. Crucially, Alpha \emph{without} its heuristic stops at $n=40$ on BSP and solves nothing at all on PUP, so lazy grounding and dynamic heuristics are separable and neither is sufficient alone: the semantics effect measured throughout this chapter is the same one that makes the reference system work.
