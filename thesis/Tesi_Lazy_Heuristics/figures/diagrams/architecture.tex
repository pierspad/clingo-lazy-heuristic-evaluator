% ---------------------------------------------------------------------------
% architecture.tex -- the propagator between clasp and the two backends.
% Used at the head of the system-architecture section.
%
% Coordinates are given explicitly (in cm) rather than through relative
% positioning, so that a box can be nudged by editing a single number.
% ---------------------------------------------------------------------------
\documentclass[border=8pt]{standalone}
\input{common.tex}

\begin{document}
\begin{tikzpicture}

% ============================== the grounder ===============================
\node[neutral, text width=30mm] (gringo) at (0, 3.2)
      {\textbf{gringo}\\[2pt]{\scriptsize ground program\\ + heuristic facts}};

% ============================== the solver =================================
\node[ground, text width=30mm, minimum height=42mm] (clasp) at (0, -0.6)
      {\textbf{clasp}\\[3pt]{\scriptsize CDNL search loop\\[5pt]
       partial assignment\\ and trail}};

\draw[flow] (gringo) -- (clasp);

% ==================== BINARY 1: clingo-native ==============================
\node[neutral, text width=42mm, minimum height=32mm] (prop_native) at (5.6, 1.8) {};
\node[font=\small\bfseries, text=diagInk, anchor=north] (title_nat)
      at ($(prop_native.north)+(0,-2.5mm)$) {HeuristicPropagator};
\node[font=\tiny\ttfamily, text=diagInk, anchor=north]
      at ($(title_nat.south)+(0,-0.5mm)$) {(Native build)};

\node[draw=diagNeutralEdge, dashed, rounded corners=2pt, fill=white,
      text width=36mm, align=center, font=\tiny, text=diagInk,
      inner sep=3pt, anchor=north] at ($(prop_native.north)+(0,-11mm)$)
      {rule AST templates\\[1pt] materialised candidates\\[1pt]
       incremental aggregate states\\[1pt] ranked targets};

\node[lazy, text width=44mm, minimum height=32mm] (native) at (11.6, 1.8)
      {\textbf{C++ backend}\\[2pt]{\scriptsize\ttfamily \_\_heuristic(\dots) facts\\[2pt]
       \normalfont\tiny C++ evaluation of weight ASTs\\ over incremental aggregates}};

\draw[biflow] ($(prop_native.east)+(0,0)$) -- ($(native.west)+(0,0)$);

\begin{scope}[on background layer]
  \node[draw=diagLazyEdge, fill=diagLazy!25, dashed, rounded corners=4pt, line width=0.8pt,
        fit=(prop_native) (native), inner sep=7pt] (box_native) {};
  \node[font=\scriptsize\bfseries\ttfamily, text=diagLazyEdge, anchor=south west]
        at (box_native.north west) {clingo-native binary};
\end{scope}

% ==================== BINARY 2: clingo-prolog ==============================
\node[neutral, text width=42mm, minimum height=32mm] (prop_prolog) at (5.6, -3.0) {};
\node[font=\small\bfseries, text=diagInk, anchor=north] (title_pro)
      at ($(prop_prolog.north)+(0,-2.5mm)$) {HeuristicPropagator};
\node[font=\tiny\ttfamily, text=diagInk, anchor=north]
      at ($(title_pro.south)+(0,-0.5mm)$) {(Prolog build)};

\node[draw=diagNeutralEdge, dashed, rounded corners=2pt, fill=white,
      text width=36mm, align=center, font=\tiny, text=diagInk,
      inner sep=3pt, anchor=north] at ($(prop_prolog.north)+(0,-11mm)$)
      {dynamic trail facts\\[1pt] SWI-Prolog term cache\\[1pt]
       query state synchronisation\\[1pt] ranked targets};

\node[lazy, text width=44mm, minimum height=32mm] (prolog) at (11.6, -3.0)
      {\textbf{Prolog backend}\\[2pt]{\scriptsize\ttfamily heuristic("\dots") strings\\[2pt]
       \normalfont\tiny in-process SWI-Prolog engine\\ mirroring the search trail}};

\draw[biflow] ($(prop_prolog.east)+(0,0)$) -- ($(prolog.west)+(0,0)$);

\begin{scope}[on background layer]
  \node[draw=diagLazyEdge, fill=diagLazy!25, dashed, rounded corners=4pt, line width=0.8pt,
        fit=(prop_prolog) (prolog), inner sep=7pt] (box_prolog) {};
  \node[font=\scriptsize\bfseries\ttfamily, text=diagLazyEdge, anchor=south west]
        at (box_prolog.north west) {clingo-prolog binary};
\end{scope}

% ==================== CALLBACK FLOWS FROM CLASP ============================
% Connections from gringo/clasp to both propagators
\draw[flow] (gringo.east) to[out=0,in=180] ($(prop_native.west)+(0,10mm)$);
\draw[flow] ($(clasp.east)+(0,12mm)$) -- ($(prop_native.west)+(0,-2mm)$)
      node[tag, midway, above, sloped] {\tiny \texttt{init}, \texttt{propagate}/\texttt{undo}};
\draw[flow] ($(prop_native.west)+(0,-10mm)$) -- ($(clasp.east)+(0,4mm)$)
      node[tag, midway, below, sloped] {\tiny \texttt{decide} $\to$ literal};

\draw[flow] ($(clasp.east)+(0,-4mm)$) -- ($(prop_prolog.west)+(0,10mm)$)
      node[tag, midway, above, sloped] {\tiny \texttt{init}, \texttt{propagate}/\texttt{undo}};
\draw[flow] ($(prop_prolog.west)+(0,2mm)$) -- ($(clasp.east)+(0,-12mm)$)
      node[tag, midway, below, sloped] {\tiny \texttt{decide} $\to$ literal};

\end{tikzpicture}
\end{document}
